# UI 问题修复计划 · v1.0.8002

> 来源：用户在桌面端（v1.0.8001）实测反馈 8 条 + 我按桌面默认窗口（**1400×900**）做的适配扫描。
> 本文只做诊断与计划，**未改动任何代码**。所有根因都带实测证据。

---

## 一、用户反馈的问题

### 问题 1 · 非全屏时启动台显示不全

| 项 | 内容 |
| --- | --- |
| 证据 | 1400×900 下启动台内容高 **1022px**，视口 900px → **溢出 122px**（柜台 265 + 告示板 412 + 页脚 57 + 标题区） |
| 根因 | 启动台按 1440×1100 的尺寸设计，未针对桌面默认窗口（900 高）做紧凑布局 |
| 修法 | ① 加高度断点 `@media (max-height: 940px)`；② 紧凑化：标题区高度与字号收敛、柜台木牌 84→70px、告示板纸条 padding/最小高度收紧、页脚单行；③ 目标：**1400×860（扣掉标题栏）内不出现纵向滚动** |
| 验证 | 在 1400×860 / 1400×900 / 1280×800 三档断言 `scrollHeight ≤ clientHeight` |

### 问题 2 · 斯诺德对决点击白屏

| 项 | 内容 |
| --- | --- |
| 证据 | 我**模拟打包布局**（只放 `斯诺德跑团/` + `poker-game/`，无 `electron-app/`）后实测：对决链接解析为 `.../斯诺德跑团/../electron-app/poker-game/index.html` → 该路径**在打包产物中不存在** |
| 根因 | 原版启动台有 `pokerGameHref()` 解析器（`window.electronAPI` → `../poker-game/index.html#/`；`/electron-app/` 路径 → `../poker-game/…`；仓库 file:// 开发 → `../electron-app/poker-game/…#/`）。我重做启动台时把它换成了 `guild-ui.js` 里的**静态 href**，且丢了 `#/` 哈希（该应用是 hash 路由） |
| 修法 | 在 `guild-ui.js` 恢复等价解析器（三分支 + `#/`），对决入口走它；保留原 `openPokerGame` 的窗口逻辑（若存在） |
| 验证 | 模拟打包布局下点击对决能正常进入游戏（断言：目标页面有游戏根节点、无 404、无空白 body） |

### 问题 3 · 职业页首页分区标题文字过黑

| 项 | 内容 |
| --- | --- |
| 证据 | 实测：`.section-title` 文字色 **rgb(67,48,26)**，与深棕木牌对比度 **2.95**（WCAG 建议 ≥4.5 ✗）；同页 `h1` 在木底上对比度 **3.30**（也偏低） |
| 根因 | `职业页/首页.html` **第 275 行还留着旧规则** `.section-title{…color:#43301a !important}`，与新加的米色规则同为 `!important` 且位置在后 → 旧规则胜出 |
| 修法 | ① 删除/改写该旧规则，统一为米色 `#f4e9d2`；② **全站"深底浅字"体检**：对所有木梁/木牌/深色条上的文字计算对比度，<4.5 的全部列出并修正；③ `h1` 加浅色描边或改用更深的墨色提升对比 |
| 验证 | 新增脚本断言：上述元素的对比度 ≥4.5（白名单除外） |

### 问题 4 · ESC 返回上一级并非处处生效

| 项 | 内容 |
| --- | --- |
| 证据 | ESC 注册分布：`advisor-widget.js`(5) `shortcuts.js`(2) `panel_engine.js`(2) `guild-ui.js`(1) `ui_dialog.js`(1) `角色创建页`(1) `角色面板`(1)；**职业页 39 页中仅极少数有 ESC** |
| 根因 | 没有统一的 ESC 语义，各页各自为政或完全没有 |
| 修法 | 在 `guild-ui.js` 建立统一 ESC 层级：① 工会面板打开 → 关闭它；② 页面自身已处理（`e.defaultPrevented` 或存在可见弹层/对话框）→ 让页面处理；③ 否则 `history.back()`；无历史时按"父级映射"回退（职业页 → 职业页首页；角色流程 → 主页；其它 → 启动台） |
| 风险 | 与 `panel_engine` 的"未保存提示"、`ui_dialog` 的确认框冲突 → 规定页面处理器优先（bubble 阶段 + 尊重 `defaultPrevented`） |
| 验证 | 新增断言：职业页/资料库/角色选择页等按 ESC 回到预期上级；面板打开时 ESC 只关面板 |

### 问题 5 · 柜台"资料库"改回"帮助页"

| 项 | 内容 |
| --- | --- |
| 证据 | `guild-ui.js` 默认 `counter = ['home','classes','library','duel']`，而告示板默认条目里也有 `library` → 重复 |
| 修法 | 默认柜台改为 `['home','classes','rulebook','duel']`（`rulebook` = `help.html`「规则手册」） |
| **兼容** | 已保存过布局的用户读到的仍是旧配置 → 需要 `v1 → v2` 迁移（版本号 +1，旧配置里若是 `library` 且告示板也有 `library`，则替换为 `rulebook`）；同时设置页给出"已更新默认入口"提示 |
| 验证 | 断言：默认柜台含 rulebook、不再含 library；旧配置读入后自动迁移且不重复 |

### 问题 6 · 角色创建页点击后突然变宽

| 项 | 内容 |
| --- | --- |
| 证据 | 1400×900 下 `body` 为栅格 `952px 380px`，但 `.container` 实测宽仅 **470px** —— 容器宽度由**当前步骤内容**决定（不同步骤内容宽度不同 → 切换步骤时跳变） |
| 修法 | ① `.container` 拉伸到栅格列宽（`width:100%` + `max-width:none` + `justify-self:stretch`）；② 卡片栅格改 `repeat(auto-fill, minmax(200px,1fr))` 填满列宽；③ 需要时给右侧概览列固定宽度 |
| 验证 | 断言：切换前 3 个步骤时 `.container` 宽度**恒定**（±2px） |

### 问题 7 · 切换大系统会关闭工会弹窗 + 名称难懂

| 项 | 内容 |
| --- | --- |
| 证据 | 实测点击第 2 个系统：`面板 open:true → false`，但系统确实切成了「职业」 |
| 根因 | 点击处理里同步重建了 `innerHTML`，被点击的 button 立刻 detach；事件冒泡到 document 时 `e.target.closest('#guiRoot')` 返回 null → 被判定为"点击面板外"而关闭 |
| 修法 | ① 关闭判断改用 `e.composedPath()`（或先判断 `rootRef.contains(target)` / 延迟一帧再渲染）；② 名称与文案调整 —— **推荐「功能导航」**（备选：系统菜单 / 快捷跳转），按钮文案与面板标题同步 |
| 验证 | 断言：连续点 5 个系统，面板始终保持打开且内容切换正确 |

---

## 二、我按桌面窗口（1400×900）实测发现的问题（问题 8）

| # | 页面 | 现象 | 建议 |
| --- | --- | --- | --- |
| 8.1 | 主页 | 纵向溢出 **36px** | 微调间距即可 |
| 8.2 | 资料库 | 纵向溢出 **98px** | 同上 |
| 8.3 | 顾问 | 纵向溢出 **18px** | 同上 |
| 8.4 | 角色创建页 | 内容列仅 **470px**，右侧留白约 480px | 与问题 6 同源，一并修 |
| 8.5 | 角色面板 | **19 个**固定/浮动元素（主题/静音/快捷键、学习技能、返回顶部…） | 逐个清点是否有遮挡内容；建议归并到右上角一列 |
| 8.6 | 职业页 | `← 返回` 与 `功能导航` 叠在木梁左上角 | **已验证可读性 OK**（黄铜牌 + 木梁），无需改 |
| 8.7 | 启动台 | 首次进入会**自动弹出更新日志**，正好盖住新界面（新用户第一印象差） | 改为：延迟 1.5s + 缩小为右下角卡片，或首次不弹、在页脚给"查看更新" |
| 8.8 | 全局 | 大量阴影/动画，未适配 `prefers-reduced-motion` | 加一条媒体查询降级 |

---

## 三、实施顺序与验证

**顺序**（按"用户可感知 + 风险低"排序）：

1. **问题 3 / 5 / 7**（纯样式与逻辑小改，1 处配置 + 1 处 CSS + 1 处事件判断）
2. **问题 2**（导航解析器，打包后不再白屏）
3. **问题 1 / 6 / 8.1-8.4**（适配与布局，需多档视口验证）
4. **问题 4**（ESC 统一，涉及多页交互，风险最高 → 放最后并单测）
5. **8.5 / 8.7 / 8.8**（体验优化）

**验证清单**：

| 类型 | 内容 |
| --- | --- |
| 既有门禁 | `verify_all`（Clean 54/54 · Errors 0）+ `verify_guild_ui_e2e`（27 断言） |
| 新增判据 | ① 启动台在 1400×860 不滚动；② 模拟打包布局下对决入口可达；③ 深底浅字对比度 ≥4.5；④ 向导页跨步骤宽度恒定；⑤ 面板点系统不关闭；⑥ ESC 回退正确 |
| 手动 | 桌面端 1400×900 与最大化各走一遍：启动台 → 对决 → 职业页 → 角色面板 → 设置 |

## 四、发布

修复完成后：`bump-version.js 1.0.8002` → 门禁 → 提交 → tag `v1.0.8002` → 推送触发 CI（桌面 + OSS + APK + Pages）→ 按 v1.0.8001 的同样方式核验产物与镜像。

> 若你希望更保守：可只发 **1.0.8002 修复版**，把 8.5/8.7/8.8 这类体验优化留到 1.0.8003。
