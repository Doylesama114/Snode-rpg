# ⚠️ 卡牌效果实现进度（临时文件）

> **时效性**：仅用于跟踪 148 张卡牌效果实现进度。全部完成后删除本文件，不再参考。
> **最后更新**：4O 完成（v1.0.6059，2026-07-04）
>
> **新会话必读（上下文接力）**：**先完整读**本文件 §0（含 **§0.6 底层规则**、**§0.7 引擎缺口**）+ `后续更新教程.md` §2 + `项目文档.md` §1.2/§12，再动手；不可跳过 build/sync/三 MD/发版步骤。

---

## §0 总体工作流程（Agent 接力用）

### 0.1 我们在做什么

在 monorepo **Snode-RPG** 的 `poker-game-main/` 模块中，把 **148 张**有效卡牌（已剔除 3 条 xlsx 模板占位）从 `conditional` 占位效果，逐步改为**结构化 effect**，并在**双引擎**中实现：

| 引擎文件 | 用途 |
|---------|------|
| `poker-game-main/src/game/effectManager.ts` | 单机 |
| `poker-game-main/server/gameEngine.js` | 联机 |
| `poker-game-main/src/data/cardDatabase.ts` | 卡数据权威源 |
| `poker-game-main/server/card-seed.json` | 由 sync 脚本生成 |

进度 tracker：**本文件**。审计产物：`poker-game-main/卡牌池审计.md`、`scripts/audit-cards.mjs`。

### 0.2 批次路线图（按顺序）

| 批次 | 状态 | 内容 |
|------|------|------|
| 阶段0 | ✅ | 148 卡基线、seed 同步、审计脚本 |
| 4A | ✅ v1.0.6043 | onField：野猪/见习冒险者/农田/铁匠铺 |
| 4B | ✅ v1.0.6044 | onReveal：收获日/雷云召来/报警机器人 |
| **4C** | ✅ v1.0.6045 | onDeploy 战术：休憩曲/祈福/炎炎夏日 + 战术 deploy 流程修复 |
| **4D** | ✅ v1.0.6046 | roundStart/End：德鲁伊/射击俱乐部/寒脊山脉 + applyRoundEffect |
| **4E** | ✅ v1.0.6047 | onField 费用/roundEnd 单目标/D6：季风/篝火/垂钓客 + getEffectivePlayCost |
| **4F** | ✅ v1.0.6048 | onGameEnd D6/翻倍 + useD6Value + 单机 round 触发修复 |
| **4G** | ✅ v1.0.6049 | d6TierPower / setPowerIfNoFieldKeyword / requireKeywords 沙滩 |
| **4H** | ✅ v1.0.6050 | searchEachKeyword / debuffOpponentHand / 酒吧女招待 searchNames |
| **4I** | ✅ v1.0.6051 | targetLeftPlayer / noHigherPowerUnitOnField / 葡萄酒商人 searchDeck |
| **4J** | ✅ v1.0.6052 | stealCard / destroy / grantUnitPlayBonus |
| **引擎债+UX** | ✅ v1.0.6053 | 环境摧毁 / 最后一轮限制 / 游戏结束浮层 / 场上悬停效果 |
| **4K** | ✅ v1.0.6054 | requireFieldName / 螃蟹 draw / 退役老兵 |
| **4L** | ✅ v1.0.6055 | countMatchingFieldCards / buffPlayedCard / 拾贝鱼人 |
| **部署引擎** | ✅ v1.0.6056 | 额外槽位规则 / grantAttributePlayBonus / setD6MinForCardName |
| **4M** | ✅ v1.0.6057 | 萨满祭司 / 珍珠商人 / 海港 |
| **4N** | ✅ v1.0.6058 | 奴隶 / 蔓生怪 / 珊瑚元素 |
| **4O** | ✅ v1.0.6059 | 猎人 / 游客 / 药剂师 |
| 4P+ | 待定 |

### 0.3 每子批严格步骤（不可跳步）

```
1. 扫描卡池（audit-cards.mjs / 卡牌池审计.md）→ 选定 3~8 张 + 是否需新引擎
2. 同步改 effectManager.ts + gameEngine.js；必要时改 src/types/game.ts
3. 改 cardDatabase.ts → node scripts/sync-card-seed.mjs
4. 构建：$env:VITE_ELECTRON="1"; pnpm run build  （在 poker-game-main/ 下）
5. 复制：robocopy dist ../electron-app/poker-game /MIR
6. 验证：node scripts/test-4b.mjs 或 test-4c.mjs；本地 http 不可靠则 pages.dev
7. node scripts/audit-cards.mjs
8. 发版：node bump-version.js 1.0.60xx "条目1;条目2"  （仓库根目录）
9. 更新 MD（全部）：本文件 + 后续更新教程.md + 项目文档.md
10. git add / commit / tag / push origin master --tags
11. 汇报用户
```

### 0.4 发版与文档规范

- **版本号**：`1.0.60xx` 末尾三位递增；用 `bump-version.js` 自动写 `changelog.js`
- **必更 MD**：`卡牌效果实现进度.md`、`后续更新教程.md`（头部版本 + §更新日志）、`项目文档.md`（§1.2 + §12）
- **勿用**：visual-engineering agent；PowerShell 直接改中文文件内容
- **线上测试**：https://snode-rpg.pages.dev/electron-app/poker-game/index.html#/new-game
- **启动台链接**：`../electron-app/poker-game/index.html`

### 0.5 当前数字（4O 完成后）

- **83/148（56.1%）**
- audit：`structured 15` / `implemented_doc 77` / `conditional_only 56`
- 最新 tag：**v1.0.6059**
- smoke：`test-4b`~`4o` + `test-engine-debt` + `test-deploy-engine` 全过

### 0.6 斯诺德对决底层规则（权威摘要，2026-07-04 用户确认）

> 来源：用户与 @D114514 确认的小程序底层运转规则。实现卡牌效果时不得与此冲突；单机/联机若未完全实现，见 §0.7。

**对局与回合**

1. **2–4 人**同局；所有玩家在同一**轮次（round）**内各自进行回合（同时决策、再同时展示）。
2. 开局：**抽 3 张手牌 + 4 点能量**；每个新回合开始：**再抽 1 张**。

**最后一轮**

3. 任意玩家己方场上放置**第 6 张牌**（**单位 + 环境**合计，不含战术占场后弃置）→ 触发**最后一轮**。
4. 最后一轮：场上 **不足 6 张**的玩家仍可继续放置单位/环境；**已有 6 张**的玩家**只能打战术牌**。**两人局**时，已有 6 张的玩家在最后一轮**完全不能出牌**。

**出牌 / 重铸（同一轮内秘密选择 → 全员确认 → 同时展示）**

5. 每名玩家秘密选「出牌」或「重铸」，全员确定后同时展示结果。
6. **出牌**：卡牌**盖放** → 全员确认 → **同时翻开** → 支付能量打出。
   - 展示前费用不足 → **不能选这张出牌**。
   - 展示后因**其他玩家效果**导致费用不足 → **仍可打出**，能量可降至**负数**。
   - **大部分**卡牌效果**不影响本批已翻开的结算**（只影响之后打出的牌；少数例外待卡面注明）。
7. **重铸**：从以下选 **2 项**（可重复）：**A** 回复 2 能量 / **B** 1 张手牌置牌库底再抽 1 / **C** 终局 **+1 战力**（bonusPower）。

**终局**

8. 结算 = 场上卡牌战力 + 终局 bonus + 特殊规则（手牌计分、任务等）。最高者胜；平局 → **先在自己场上放满 6 张**的玩家胜。

**卡牌类型（数据层）**

| 类型 | `CardType` | 要点 |
|------|------------|------|
| 单位牌 | `unit` | 进场后**留场**，有战力，计入 6 张上限 |
| 环境牌 | `environment` | 进场后**留场**，持续/onField/round 效果；**计入 6 张上限** |
| 战术牌 | `tactic` | 翻开 → 效果 → **进弃牌堆**，不占终局 6 槽（短暂占槽后清空） |
| 「建筑」 | 关键词 `建筑` | **不是**独立类型；多为 `environment` + `keywords` 含 `建筑` |

**效果时机（与类型的关系）**

- **战术牌**：`onDeploy`（若有）→ `onReveal` → 弃置；卡面「揭示」= 战术的 onReveal。
- **单位 / 环境**：进场时一次性效果；卡面常写「揭示」，引擎中可能是 `onDeploy` 或 `onReveal`，语义均为**进场触发**，非战术式弃置流程。

### 0.7 引擎缺口与实现策略

| 缺口 | 规则关联 | 状态 |
|------|----------|------|
| 同时盖牌 / 批次翻开 | §0.6 规则 5–6 | ⏳ 待做（高） |
| 展示前 vs 展示后费用校验 | 规则 6 | 部分已支持 |
| 最后一轮 6 张限制 | 规则 4 | ✅ v1.0.6053 单机+联机 |
| 环境牌可被摧毁 | 类型 + 力场波 | ✅ v1.0.6053 `canDestroyTarget` |
| `unitPlayPowerBonus` 持续时间 | 气泡酒 | ✅ v1.0.6053 回合末清除 |
| 手牌/任务/onGameEnd 计分 | 规则 8 | 按卡推进 |
| 单位 deploy-on-card / 速攻 | 多种卡 | ✅ v1.0.6056 槽位规则+部署加成+D6下限 |
| 游戏结束返回按钮 | UX | ✅ v1.0.6053 固定浮层 |
| 场上卡牌效果可见 | UX | ✅ v1.0.6053 悬停弹窗 |

**当前批次策略（4K+）**

- **可继续**：结构化 `conditional` → 已有 effect 类型，不依赖盖牌批次的新卡。
- **应先补引擎再打数据**：依赖「本批 reveal 隔离」的牌 → 先改盖牌批次再写 cardDatabase。
- 已完成的 4A–4J：效果语义与类型分工基本一致；与 §0.6 的全局流程（同时展示、批次隔离）尚未完全对齐，属**流程层**债务。

---

## 当前状态

**83/148 张已实现（56.1%）** — 4O：猎人 / 游客 / 药剂师

> 脚本：`test-4b.mjs` … `test-4o.mjs`、`test-engine-debt.mjs`、`test-deploy-engine.mjs`

| 文件 | 角色 |
|------|------|
| `poker-game-main/server/gameEngine.js` | 服务端引擎（联机模式用） |
| `poker-game-main/src/game/effectManager.ts` | 客户端引擎（单机模式用） |
| `poker-game-main/src/data/cardDatabase.ts` | 148 张卡数据（引擎共享） |
| `poker-game-main/server/card-seed.json` | 服务端卡数据种子 |

---

## 已实现效果

| 批次 | 版本 | 卡牌 | 效果类型 | 数量 |
|------|------|------|---------|------|
| 1 | v1.0.6015 | 挤奶工/水元素/火元素/土元素/大海/热带鱼 | onOtherPlay/onField modifyPower | 6 | ✅ |
| 2a | v1.0.6016 | 狗头人冒险者/圣洁骑士 | selfTarget + invertCondition | 2 | ✅ |
| 2b | v1.0.6017 | 落魄男爵 | selfTarget + stackable 计数 | 1 | ✅ |
| 2c | v1.0.6018 | 蜥蜴人勇士/森林 | requireKeywords 复合条件 | 2 | ✅ |
| 2d | v1.0.6019 | 马车/海藻 | 名称匹配 modifyPower | 2 | ✅ |
| 3a | v1.0.6020 | 沙滩碉堡 | protect | 1 | ✅ |
| 3b | v1.0.6022 | 护甲药水/力场波 | protect/destroy + 摧毁引擎 | 2 | ✅ |
| 3c | v1.0.6023 | 集群战术/蓝焰术 | onReveal modifyPower | 2 | ✅ |
| 3d | v1.0.6024 | 摄像机/鲈鱼/酒馆/法师塔 | createSlot | 4 | ✅ |
| 3e | v1.0.6033 | 活力药水/真气波 | quickPlay restoreEnergy/modifyPowerByName | 2 | ✅ |
| 3f | v1.0.6034 | 急速射击/奇袭 | quickPlay reduceUnitPower/discardOpponentHand | 2 | ✅ |
| 3g | v1.0.6035 | 奥术箭/元素墙/牛奶/创造水源/火焰箭 | quickPlay returnToDeckBottom/setNextUnitAttribute/markOpponentHand | 5 | ✅ |
| 5 | v1.0.6042 | 水元素/火元素/土元素 | onOtherPlay selfTarget + generic handler | 3 | ✅ |
| 4A | v1.0.6043 | 野猪/见习冒险者/农田/铁匠铺 | selfTarget 累加 + countUniqueKeywords + countDeployedOnSelf | 4 | ✅ |
| 4B | v1.0.6044 | 收获日/雷云召来/报警机器人 | onReveal searchDeck + allPlayers modifyPower + extraPlay | 3 | ✅ |
| 4C | v1.0.6045 | 休憩曲/祈福/炎炎夏日 | onDeploy restoreEnergy + absNegativePower + setFieldAttribute | 3 | ✅ |
| 4D | v1.0.6046 | 德鲁伊/射击俱乐部/寒脊山脉 | round requireField + searchDeck + 全场属性 modify | 3 | ✅ |
| 4E | v1.0.6047 | 季风/篝火/垂钓客 | modifyPlayCost + getEffectivePlayCost + d6Min + targetOtherOnField | 3 | ✅ |
| 4F | v1.0.6048 | 吟游诗人/风笛/激励乐章 | onGameEnd d6/double + useD6Value + round 切换修复 | 3 | ✅ |
| 4G | v1.0.6049 | 贝壳/晴天/沙滩 | d6TierPower + setPowerIfNoFieldKeyword + requireKeywords | 3 | ✅ |
| 4H | v1.0.6050 | 酒吧女招待/杂货铺/燃烧之手 | searchEachKeyword + debuffOpponentHand + searchNames | 3 | ✅ |
| 4I | v1.0.6051 | 葡萄酒商人/魔法飞弹/暴徒 | targetLeftPlayer + noHigherPowerUnitOnField + searchDeck | 3 | ✅ |
| 4J | v1.0.6052 | 海鸥/精准射击/气泡酒 | stealCard + destroy + grantUnitPlayBonus | 3 | ✅ |
| 4K | v1.0.6054 | 奶牛/螃蟹/退役老兵 | requireFieldName + draw + noOtherFieldKeyword | 3 | ✅ |
| 4L | v1.0.6055 | 海葵/翻车鱼/拾贝鱼人 | countMatchingFieldCards + buffPlayedCard | 3 | ✅ |
| 部署引擎 | v1.0.6056 | （纯引擎，无卡数据） | createSlot 规则 + grantAttributePlayBonus + setD6MinForCardName | 0 | ✅ |
| 4M | v1.0.6057 | 萨满祭司/珍珠商人/海港 | grantAttributePlayBonus + setD6MinForCardName + createSlot 槽位规则 | 3 | ✅ |
| 4N | v1.0.6058 | 奴隶/蔓生怪/珊瑚元素 | excludeFromFieldCount + conditionalPlayCost + requireAllFieldAttributes | 3 | ✅ |
| 4O | v1.0.6059 | 猎人/游客/药剂师 | conditionalPlayCost(0) + modifyPowerByUniqueAttributes + grantTacticPlayFree | 3 | ✅ |

> **4O 引擎增强**：复用 `conditionalPlayCost`（猎人免费）；`modifyPowerByUniqueAttributes`+`includeHand`（游客 onGameEnd）；`grantTacticPlayFree`+`consumeTacticPlayFreeIfMatch`（药剂师战术不占用行动）。

> **4N 引擎增强**：`excludeFromFieldCount`（奴隶 onDeploy）；`conditionalPlayCost`+`playCostValue`（蔓生怪）；`requireAllFieldAttributes`/`requireFieldAttributes`（珊瑚元素）；`countMainFieldCardsForLimit` 终局 6 张计数排除。

> **4M**：萨满祭司 `grantAttributePlayBonus`（风/火/水/土各 +1）；珍珠商人 `setD6MinForCardName`（贝壳 D6≥5 珍珠档）；海港 `createSlot`+`slotDeployKeywords:['船']`+`+3`+不计终局；狮鹫已有基础 createSlot 无需改动。

> **部署引擎 v1.0.6056**：`SlotDeployRules` / `canDeployOnExtraSlot` / `applyExtraSlotDeployModifiers`（海港船槽 +3 不计数；狮鹫任意单位槽）；`applyUnitDeployBonuses` 统一单位部署加成；`grantAttributePlayBonus` / `setD6MinForCardName`；`getAvailableSlotIndices` 接入 SP/MP/Client；`test-deploy-engine.mjs` 18/18。

> **4L 引擎增强**：`countMatchingFieldCards`+`maxBasePower`（海葵 onDeploy）；onOtherPlay `buffPlayedCard`+`triggerCount`（翻车鱼）；拾贝鱼人补 `shuffleAfterSearch`。

> **4K 引擎增强**：`checkFieldRequirements` / `requireFieldName`（奶牛/螃蟹）；onReveal `draw`；onDeploy `noOtherFieldKeyword`+`requireOtherFieldKeyword`（退役老兵）。

> **4A 引擎增强**：`applySelfTargetFieldEffects` — 多效果累加、`countUniqueKeywords`（见习冒险者）、`countDeployedOnSelf`+`replacePowerWithDeploymentCount`（农田）；移除四张卡硬编码。

> **4B 引擎增强**：`applyRevealEffect` — 多 onReveal 效果链式执行；`searchKeywords`/`maxCount`/`shuffleAfterSearch`（收获日）；`allPlayers`+`excludeAttributes`（雷云召来）；onReveal `extraPlay`（报警机器人）。

> **4C 引擎增强**：`applyDeployEffect` — 统一 onDeploy；`restoreEnergy`（休憩曲）、`absNegativePower`（祈福）、`setFieldAttribute`+`allPlayers`（炎炎夏日）；战术牌 **先 onDeploy 再 onReveal**。

> **4D 引擎增强**：`applyRoundEffect` — `requireFieldKeywords`（德鲁伊）、roundStart `searchDeck`（射击俱乐部）、roundEnd 全场 `targetAllCards`+属性过滤（寒脊山脉）。

> **4E 引擎增强**：`getEffectivePlayCost` — onField `modifyPlayCost`（季风）；`applyRoundEffect` — `targetOtherOnField`+`excludeSelf`（篝火）、`d6Min`（垂钓客）；出牌费用校验接入单机/联机/客户端。

> **4F 引擎增强**：`triggerGameEndEffects` — `d6ModifyPower`（吟游诗人）、`doubleTargetPower`（风笛）；`applyRevealEffect` — `useD6Value`（激励乐章）；**修复** `useGameNew.switchToNextPlayer` roundEnd/Start 永不触发。

> **4G 引擎增强**：`d6TierPower` + `rollD6TierValue`（贝壳 quickPlay/onReveal）；`setPowerIfNoFieldKeyword`（晴天 onGameEnd）；沙滩复用 `requireKeywords` onField（无新逻辑）。

> **4H 引擎增强**：`searchDeck` — `searchEachKeyword`（杂货铺分关键词各检一张）、`searchNames`（酒吧女招待）；`debuffOpponentHand` — 左手边玩家随机手牌 basePower 削弱（燃烧之手）。

> **4I 引擎增强**：`modifyCost` — `targetLeftPlayer`（魔法飞弹仅左手边）；`applyDeployEffect` — `noHigherPowerUnitOnField`（暴徒条件自增）；葡萄酒商人复用 `searchDeck`+`searchName`。

> **4J 引擎增强**：`applyRevealEffect` — `stealCard`+`targetLeftPlayer`（海鸥）、`grantUnitPlayBonus`（气泡酒）；`applyDestroyToTarget` / onDeploy+onReveal `destroy`（精准射击 -2 降至 0 摧毁）；单位 onReveal 统一走 `applyRevealEffect`。

---

## 4O 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-4o.mjs` | ✅ 9/9 |
| 发版 | v1.0.6059 |

---

## 4N 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-4n.mjs` | ✅ 9/9 |
| `node scripts/audit-cards.mjs` | ✅ implemented_doc 74 / conditional 59 |
| 发版 | v1.0.6058 |

---

## 4M 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-4m.mjs` | ✅ 13/13 |
| `node scripts/audit-cards.mjs` | ✅ implemented_doc 71 / conditional 62 |
| 发版 | v1.0.6057（待 commit/tag） |

---

## 部署引擎验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-deploy-engine.mjs` | ✅ 18/18 |
| `test-4b`~`4l` + `test-engine-debt` | ✅ 全过 |
| `npm run build` + robocopy | ✅ |
| 发版 | v1.0.6056（待 commit/tag） |

---

## 4L 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-4l.mjs` | ✅ 8/8 |
| `node scripts/audit-cards.mjs` | ✅ structured 21 / conditional 65 |
| 发版 | v1.0.6055 |

---

## 4K 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-4k.mjs` | ✅ 8/8 |
| `node scripts/audit-cards.mjs` | ✅ structured 19 / conditional 67 |
| 发版 | v1.0.6054 |

---

## 4J 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-4j.mjs` | ✅ 6/6 |
| `node scripts/audit-cards.mjs` | ✅ implemented_doc 62 |
| 发版 | v1.0.6052 |

---

## 4I 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-4i.mjs` | ✅ 7/7 |
| `node scripts/audit-cards.mjs` | ✅ implemented_doc 59 / conditional 69 |
| 发版 | v1.0.6051 |

---

## 4H 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-4h.mjs` | ✅ 8/8 |
| `node scripts/audit-cards.mjs` | ✅ structured 27 / conditional 71 |
| 发版 | v1.0.6050 |

---

## 4G 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-4g.mjs` | ✅ 7/7 |
| `node scripts/audit-cards.mjs` | ✅ structured 26 / conditional 73 |
| 发版 | v1.0.6049 |

---

## 4F 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-4f.mjs` | ✅ 8/8 |
| `node scripts/audit-cards.mjs` | ✅ conditional 76（-2） |
| 发版 | v1.0.6048 |

---

## 4E 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-4e.mjs` | ✅ 7/7 |
| `node scripts/audit-cards.mjs` | ✅ structured 25 / conditional 78 |
| 发版 | v1.0.6047 |

---

## 4D 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-4d.mjs` | ✅ 4/4 |
| `node scripts/audit-cards.mjs` | ✅ implemented_doc 44 / conditional 80 |
| 发版 | v1.0.6046 |

---

## 4C 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/test-4c.mjs` | ✅ 7/7 |
| `node scripts/audit-cards.mjs` | ✅ implemented_doc 41 / conditional 83 |
| 发版 | v1.0.6045 |

---

## 4B 验证记录（2026-07-04）

| 步骤 | 结果 |
|------|------|
| `node scripts/sync-card-seed.mjs` | ✅ 148 条 |
| `$env:VITE_ELECTRON="1"; pnpm run build` | ✅ |
| `node scripts/audit-cards.mjs` | ✅ structured 24 / implemented_doc 38 / conditional 86 |
| `node scripts/test-4b.mjs` | ✅ 9/9 引擎 smoke 通过 |
| 本地 `python -m http.server 3456` | ❌ 连接被关闭（不可靠） |
| GitHub Pages 部署 | ✅ `https://snode-rpg.pages.dev/electron-app/poker-game/index.html#/new-game`（cardDatabase 含 4B 结构化数据） |
| 启动台链接 | ✅ 已修正为 `../electron-app/poker-game/index.html` |

---

## 引擎已支持的效果类型

| 类型 | 参数 | 说明 |
|------|------|------|
| `modifyPower` | `value`, `targetKeywords` | 修改战力 |
| `modifyPower` + `selfTarget` | `value`, `targetKeywords` | 自改战力（检查自身场上） |
| `modifyPower` + `allPlayers` | `excludeAttributes` / `targetAttributes` | 全场单位战力修改（雷云召来） |
| `modifyPower` + `invertCondition` | `value`, `targetKeywords` | 逆向条件（场上无 X 时触发） |
| `modifyPower` + `stackable` | `value`, `targetKeywords` | 每匹配一张 +value |
| `modifyPower` + `requireKeywords` | `value`, `[[A],[B]]` | 多个关键词组全部满足 |
| `modifyPower` + `countUniqueKeywords` | `value`, `selfTarget` | 场上不同关键词种类数 × value |
| `modifyPower` + `countDeployedOnSelf` | `targetKeywords`, `value` | 部署在本卡额外槽的单位数 |
| `searchDeck` | `searchKeywords`, `maxCount`, `shuffleAfterSearch` | 多关键词检索 + 洗牌 |
| `extraPlay` | — | onDeploy / onReveal 额外出牌 |
| `restoreEnergy` | `value` | onDeploy / quickPlay 回复能量 |
| `absNegativePower` | — | onDeploy 负数战力变正（祈福） |
| `setFieldAttribute` | `value`, `targetCardType`, `allPlayers` | onDeploy 改变场上属性（炎炎夏日） |
| `modifyPlayCost` | `value`, `targetAttributes`, timing `onField` | 在场时减免打出费用（季风） |
| `modifyPower` + `targetOtherOnField` | `excludeSelf`, `targetCardType`, `excludeAttributes` | roundEnd 改场上其他单卡（篝火） |
| `draw` + `d6Min` | `drawCount`, `d6Min` | roundStart D6 条件抽牌（垂钓客） |
| `d6ModifyPower` | timing `onGameEnd` | 游戏结束掷 D6 加战力（吟游诗人） |
| `doubleTargetPower` | `targetName`, timing `onGameEnd` | 游戏结束按名称翻倍（风笛） |
| `modifyPower` + `useD6Value` | `targetKeywords`, timing `onReveal` | 揭示时用 D6 作为增量（激励乐章） |
| `d6TierPower` | `d6Tiers`, timing `onReveal` | D6 档位加战力（贝壳） |
| `setPowerIfNoFieldKeyword` | `targetKeywords`, `value`, timing `onGameEnd` | 无其他关键词时设战力（晴天） |
| `destroy` | `targetKeywords` | 摧毁场上匹配卡牌 |
| `modifyPowerByName` | `targetName`, `value` | quickPlay 名称匹配修改战力 |

---

## 待实现分类

| 类别 | 待做卡数 | 说明 |
|------|---------|------|
| modifyPower（条件复杂） | ~33 | 需新引擎特性（数值比较/NOT条件/多条件等） |
| createSlot | ~10 | 创建额外槽位 |
| destroy | ~5 | 摧毁卡牌 |
| draw/search | ~2 | 抽牌/检索（收获日已完成） |
| deploy on card | ~13 | 部署在其他卡牌上 |
| tactic/速攻 | ~17 | 战术/速攻类即时效果 |
| 回合事件 | ~15 | 每回合触发 |
| 其他 | ~33 | D6 随机/联机互动/特殊机制 |

---

## 全流程步骤（每次子批严格遵循）

```
1. 扫描卡池 → 确定本子批实现哪些卡 + 需不需要新引擎特性
2. 如需新引擎：同步改 server/gameEngine.js + src/game/effectManager.ts
3. 改 src/data/cardDatabase.ts 的卡牌 effect 结构
4. 重建：$env:VITE_ELECTRON="1"; pnpm run build
5. 复制 dist：robocopy dist ../electron-app/poker-game /MIR
6. 验证：`node scripts/test-4b.mjs` / `test-4c.mjs` / `test-4d.mjs`（或 pages.dev）
7. Git commit（代码改动，不发版）
8. 发版：node bump-version.js → 自动写 changelog.js
9. 更新根目录 MD：`后续更新教程.md` + `项目文档.md` + 本文件进度
10. git add/commit/tag/push
11. 汇报用户
```

## 关键提醒

1. **每次修改引擎必须同步两个文件**：`server/gameEngine.js` + `src/game/effectManager.ts`
2. **每次改完 cardDatabase.ts 后必须重建**：`$env:VITE_ELECTRON="1"; pnpm run build`
3. **deploy.yml 已包含** `electron-app/poker-game` → `_deploy/poker-game`（Pages 可访问 `/poker-game/`）
4. **版本号规则**：只改末尾三位，大版本等用户指令
5. **更新本文件是子批的最后一步**，不得遗漏
6. **发版时同步更新** `后续更新教程.md`、`项目文档.md`（见 `后续更新教程.md` §2 步骤3）
