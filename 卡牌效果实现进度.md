# ⚠️ 卡牌效果实现进度（临时文件）

> **时效性**：仅用于跟踪 151 张卡牌效果实现进度。全部完成后删除本文件，不再参考。
> **最后更新**：v1.0.6025

---

## 当前状态

**22/151 张已实现（14.6%）**

| 文件 | 角色 |
|------|------|
| `poker-game-main/server/gameEngine.js` | 服务端引擎（联机模式用） |
| `poker-game-main/src/game/effectManager.ts` | 客户端引擎（单机模式用） |
| `poker-game-main/src/data/cardDatabase.ts` | 151 张卡数据（引擎共享） |
| `poker-game-main/server/card-seed.json` | 服务端卡数据种子 |

---

## 已实现效果

| 批次 | 版本 | 卡牌 | 效果类型 | 数量 |
|------|------|------|---------|------|
| 1 | v1.0.6015 | 挤奶工/水元素/火元素/土元素/大海/热带鱼 | onOtherPlay/onField modifyPower | 6 | ✅ |
| 2a | v1.0.6016 | 狗头人冒险者/圣洁骑士 | selfTarget + invertCondition | 2 | ✅ |
| 2b | v1.0.6017 | 落魄男爵 | selfTarget + stackable 计数 | 1 | ✅ |
| 2c | v1.0.6018 | 蜥蜴人勇士/森林 | requireKeywords 复合条件 | 2 | ✅ |
| 2d | v1.0.6019 | 马车/海藻 | 名称匹配 modifyPower | 2 | ✅ |
| 3a | v1.0.6020 | 沙滩碉堡 | protect | 1 | ✅ |
| 3b | v1.0.6022 | 护甲药水/力场波 | protect/destroy + 摧毁引擎 | 2 | ✅ |
| 3c | v1.0.6023 | 集群战术/蓝焰术 | onReveal modifyPower | 2 | ✅ |
| 3d | v1.0.6024 | 摄像机/鲈鱼/酒馆/法师塔 | createSlot | 4 | ✅ |

> **验证**：✅ = Playwright 通过（游戏启动/出牌/部署无崩溃，0 控制台错误）

---

## 引擎已支持的效果类型

| 类型 | 参数 | 说明 |
|------|------|------|
| `modifyPower` | `value`, `targetKeywords` | 修改战力 |
| `modifyPower` + `selfTarget` | `value`, `targetKeywords` | 自改战力（检查自身场上） |
| `modifyPower` + `invertCondition` | `value`, `targetKeywords` | 逆向条件（场上无 X 时触发） |
| `modifyPower` + `stackable` | `value`, `targetKeywords` | 每匹配一张 +value |
| `modifyPower` + `requireKeywords` | `value`, `[[A],[B]]` | 多个关键词组全部满足 |
| `destroy` | `targetKeywords` | 摧毁场上匹配卡牌 |

---

## 待实现分类

| 类别 | 待做卡数 | 说明 |
|------|---------|------|
| modifyPower（条件复杂） | ~36 | 需新引擎特性（数值比较/NOT条件/多条件等） |
| createSlot | ~10 | 创建额外槽位 |
| destroy | ~5 | 摧毁卡牌 |
| draw/search | ~5 | 抽牌/检索 |
| deploy on card | ~13 | 部署在其他卡牌上 |
| tactic/速攻 | ~20 | 战术/速攻类即时效果 |
| 回合事件 | ~15 | 每回合触发 |
| 其他 | ~33 | D6 随机/联机互动/特殊机制 |

---

## 引擎升级计划（按顺序执行）

| # | 模块 | 改什么 | 影响卡数 | 状态 |
|---|------|--------|---------|------|
| 1 | 属性匹配 | `hasAnyKeyword` 加 `card.attribute` 检查 + `targetAttributes` 字段 | ~20 | ✅ v1.0.6030 |
| 2 | 检索/抽牌 | `searchDeck(player, keyword)` | ~10 | ❌ |
| 3 | 速攻 | deploy/reveal 流程加 `quickPlay` 分支 | ~15 | ❌ |
| 4 | 回合事件 | `startNewRound` 加 `triggerRoundEffects` | ~10 | ❌ |
| 5 | D6 随机 | `rollD6()` + 分支效果 | ~5 | ❌ |
| 6 | 偷取 | `stealPower(source, target, amount)` | ~5 | ❌ |

### 引擎文件（每次改两个）

| 文件 | 角色 |
|------|------|
| `poker-game-main/server/gameEngine.js` | 服务端引擎 |
| `poker-game-main/src/game/effectManager.ts` | 客户端引擎 |

### 升级流程

```
1. 同步改 server/gameEngine.js + src/game/effectManager.ts
2. 改 src/types/game.ts（新增 type/字段）
3. 扫描 card-seed.json → 找到受益卡
4. 批量转换 cardDatabase.ts
5. 重建 → Playwright 验证 → 发版 → 更新本文件
```

---

## 引擎仍需支持的效果类型（待实现）

| 类型 | 说明 | 影响卡数 |
|------|------|---------|
| 属性匹配 | 按元素属性(火/水/木等)而非关键词匹配 | ~20 |
| 偷取战力 | 从其他卡偷取点数 | ~5 |
| 检索/抽牌 | 从牌库搜特定卡 | ~10 |
| 速攻 | 即时效果，不占动作 | ~15 |
| D6随机 | 掷骰子随机效果 | ~5 |
| 回合事件 | 每回合自动触发 | ~10 |
| 其他 | 复杂条件/多效果/联机互动 | ~30 |

---

## 全流程步骤（每次子批严格遵循）

```
1. 扫描卡池 → 确定本子批实现哪些卡 + 需不需要新引擎特性
2. 如需新引擎：同步改 server/gameEngine.js + src/game/effectManager.ts
3. 改 src/data/cardDatabase.ts 的卡牌 effect 结构
4. 重建：$env:VITE_ELECTRON="1"; pnpm run build
5. 复制 dist：Remove-Item ../electron-app/poker-game; Copy-Item dist ../electron-app/poker-game
6. Playwright 验证：
   a. 启动本地服务器：python -m http.server 3456 -d ../electron-app/poker-game
   b. 导航到 http://localhost:3456/#/new-game
   c. 开始游戏 → 至少部署一张卡 → 检查无控制台错误
   d. 截图作为证据
   e. 停止服务器
7. Git commit（不发版）
8. 发版：node bump-version.js → git add/commit/tag/push
9. 更新本文件进度
10. 汇报用户
```

## 关键提醒

1. **每次修改引擎必须同步两个文件**：`server/gameEngine.js` + `src/game/effectManager.ts`
2. **每次改完 cardDatabase.ts 后必须重建**：`$env:VITE_ELECTRON="1"; pnpm run build`
3. **子批完成后**：发布新版本 → 更新本文件进度 → 汇报用户
4. **不要用 visual-engineering agent**（有 bug，不可用）
5. **版本号规则**：只改末尾三位，大版本等用户指令
6. **更新本文件是子批的最后一步**，不得遗漏
