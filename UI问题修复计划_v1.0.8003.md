# UI 问题修复计划 · v1.0.8003

> 来源：用户 5 条反馈（规则手册表格 / 设置页 ESC / 首页检索弹窗 / 职业页筛选弹窗 / 其他弹窗风格）。
> 本文只做诊断与计划，**未改动任何代码**；每条都有实测证据。

---

## 一、规则手册表格显示不全

| 项 | 内容 |
| --- | --- |
| 现象 | 「职业兼容性」等表格右侧被截断/挤压 |
| 证据 | `help.html` 自身：`.container{max-width:1200px}`、`.wrap{overflow-x:auto;max-width:100%}`；而 `guild-ui.css:306` 的适配器写了 `body.gui-page .container, body.gui-page .wrap { max-width:980px }` → **容器被从 1200px 压到 980px**；表格又是 `.wrap.table-fame table{table-layout:fixed}` + 百分比列宽（intro 38% / pos 24% / neg 24%）→ 内容被硬挤 |
| 修法 | ① 该页容器宽度**恢复原本的 1200px**（给它一个页面级放行，如 `html.gui-help-page body.gui-page .container{max-width:1200px}`）；② 给表格容器加横向滚动兜底（`overflow-x:auto` 已有，但需确保 `min-width` 不被压）；③ 其余用适配器的页面保持 980px 不变 |
| 验证 | 断言：help.html 页面宽 ≥1200px；职业兼容性表格 `scrollWidth ≤ clientWidth`（无截断） |

## 二、设置页 ESC 不能返回上一级

| 项 | 内容 |
| --- | --- |
| 证据 | `guild-ui.js` 的 `boot()`：第 537 行 `themeBoot()` → 第 538 行 `if (window.__guiNoNav) return;` → 第 541 行 `escBoot()`。设置页设了 `__guiNoNav=true`，于是在 `escBoot` 之前就 return 了 → **ESC 层根本没注册**（与上一轮 themeBoot 的同类缺陷） |
| 修法 | 把 `escBoot()` 与 `themeBoot()` 一起提到提前 return **之前**；`__guiNoNav` 只应关闭**导航面板**（`navBoot`），不应影响 ESC/主题/皮肤 |
| 验证 | 断言：设置页按 ESC → 回到启动台 |

## 三、首页检索弹窗风格

| 项 | 内容 |
| --- | --- |
| 证据（实测计算样式） | `.search-panel` 底 `rgb(253,248,236)`（羊皮纸 ✓）、圆角 14px；但**头部、chips、输入框、清空链接都是旧样式**：无木牌标题栏、无黄铜关闭钮、chips 为浅灰白圆角、"清空" 为纯文本链接 |
| 修法 | 套用统一弹窗规范（见第五节）：头部→木牌标题栏 + 黄铜圆形关闭钮；分组标题→深木/黄铜条；chips→黄铜描边胶囊；输入框→羊皮纸底 + 木色描边 |

## 四、职业页筛选弹窗风格

| 项 | 内容 |
| --- | --- |
| 证据（截图 + 计算样式） | `.fp-body` 底 `rgb(253,248,236)` + 圆角 12px + 暖描边（材质已统一 ✓），但：标题「技能筛选」是**纯文字**（无木牌标题栏）、分组标题「· 类型 3」「· 其他关键词 130」是**浅赭色条**、"清空" 是纯文本、chips 白底 |
| 修法 | 同第三节：加木牌标题栏 + 分区标题条 + 黄铜 chips；折叠态按钮 `.fp-toggle`（⚙ 筛选 + 计数徽标）改为黄铜牌，与「功能导航」一致 |

## 五、其他弹窗全站体检（我逐个截图 + 取样式指纹）

| # | 弹窗 | 位置 | 现状 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | 首页检索 `.search-panel` | 职业页/首页.html | 羊皮纸底，但结构全旧 | ✗ 需统一 |
| 2 | 职业页筛选 `.fp-body` | 职业页/filter-panel.js | 羊皮纸底，标题/分组条旧 | ✗ 需统一 |
| 3 | **角色面板商店弹窗** | 角色面板.html `openStore()` | **纯白面板 + 灰色 chips + 白列表行** | ✗ 最明显 |
| 4 | 快捷键帮助 `#_shortcutHelp` | shortcuts.js | 象牙白面板、无木牌标题 | ✗ 需统一 |
| 5 | 更新日志弹窗 | changelog.js（**全内联样式**，无类名） | 纯白 + 灰标题 | ✗ 需统一 |
| 6 | 通用确认框 | ui_dialog.js（**全内联样式**） | 纯白 + 默认按钮 | ✗ 需统一 |
| 7 | 首页同页预览浮层 `.gs-preview` | 职业页/首页.html | 深色工具条 `#2d2722` + 黄铜按钮 | ✗ 与其他不一致 |
| 8 | 移动端导航抽屉 `.nav-drawer` | 职业页/common.css | 羊皮纸 + 暖描边 | ✓ 已统一 |
| 9 | 角色面板：学习技能面板 / 演练场 / 导出对话框 | 角色面板.html | 待逐一截图确认 | ⏳ 待查 |
| 10 | 角色创建页：各类确认/提示 | 角色创建页.html | 待确认 | ⏳ 待查 |

## 六、统一方案：定义「弹窗组件规范」并复用

在 `guild-ui.css` 增加一组**可复用的弹窗类**（与已有的 `.gui-panel` 同源）：

| 组件 | 规格 |
| --- | --- |
| `.gui-modal` | 外层：深木框（`inset 高光 + 底暗边`）+ 大投影；圆角 9px |
| `.gui-modal-head` | 木牌标题栏：深木渐变 + 米色衬线标题 + 金色拉丁副标 + **黄铜圆形关闭钮** |
| `.gui-modal-body` | 羊皮纸内页（`inset` 内阴影）+ 内边距 |
| `.gui-group-title` | 分区标题条：深木底 + 米色字（对比度 ≥4.5） |
| `.gui-chip` / `.gui-chip.on` | 黄铜胶囊：未选=羊皮纸+木描边；选中=深木底+米色字 |
| `.gui-input` | 羊皮纸底 + 木色描边，focus 转黄铜 |

**接入方式**（分两类）：
- **有稳定类名/ID**的（1、2、4、7、8）→ 只用 CSS（`guild-ui.css` 内加作用域规则），**不改 JS**；
- **纯内联样式**的（3 商店、5 更新日志、6 确认框）→ 需要改 JS：把它们的内联样式抽成类名并套用上述组件（改动集中在各自的 build 函数）。

## 七、实施顺序与验证

1. **问题一（表格宽度）+ 问题二（ESC）** —— 各一处配置/顺序修正，风险最低
2. **弹窗规范落地 + 接入 1/2/4/7**（纯 CSS，可一次做完）
3. **接入 3/5/6**（改 JS，逐个验证不破坏交互）
4. **补查 9/10**（截图确认，必要时一并统一）

**验证清单**：
| 类型 | 内容 |
| --- | --- |
| 既有门禁 | `verify_all`（Clean 54/54 · Errors 0）+ `verify_ui_8002_e2e`（20 断言）+ `verify_guild_ui_e2e`（27 断言） |
| 新增断言 | ① help 页宽 ≥1200 且表格无截断；② 设置页 ESC 回启动台；③ 7 个弹窗的底色为羊皮纸/木色且**存在标题栏**；④ 弹窗内文字对比度 ≥4.5 |
| 手动 | 逐个打开弹窗截图，与「功能导航」面板并排比对风格 |

## 八、发布

修完 → `bump-version.js 1.0.8003` → 门禁 → 发版前截图核验 → tag `v1.0.8003` → CI 三端 + 产物/镜像核验（流程同 v1.0.8002）。
