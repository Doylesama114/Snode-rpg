# UI 接入更新计划（v7 定稿 → 正式软件）

> 定稿方向见 `设计风格规范.md`；草案源码见 `测试型更新/导航草案v7/`。
> 日期：2026-09-20 · 基线版本：v1.0.7279

---

## 0. 备份（已完成/进行中）

| 项 | 内容 |
| --- | --- |
| 代码快照 | `备份区/2026-09/软件整体备份_v1.0.7279_20260920.zip`（斯诺德跑团 / 职业页 / electron-app / mobile / scripts / docs / poker-game-main / music + 根目录文件） |
| Git | 当前提交 `7b714cc`，tag `v1.0.7279` 已存在 |
| 回滚方式 | 解压 zip 覆盖，或 `git reset --hard <tag>` |

---

## 1. 反思：当前草案存在的问题

### 1.1 你已提出的（必须解决）

| # | 问题 | 影响 |
| --- | --- | --- |
| **P1** | **跳转缺上下文**：角色存档 / 角色面板在"没有选中角色"时直接跳过去，页面无内容可显示 | 用户点了等于白点，逻辑不成立 |
| **P2** | **告示板内容不可自定义**：只能显示我们写死的入口，用户想固定"我的某个角色"做不到 | 无法满足"个性化快捷入口"的核心诉求 |

### 1.2 我自查出来的（建议一并处理）

| # | 问题 | 说明 |
| --- | --- | --- |
| P3 | **返回语义断裂**：跨页跳转后，目标页的「← 返回」回到的是它原来的首页，不是启动台 | 用户"跳过去就回不来" |
| P4 | **自定义后没有溢出策略**：条目数不定，告示板会被撑爆 | 需要上限（如 12 项）+「更多」入口 |
| P5 | **设置项全是空壳**：音量/主题/自动更新/检查更新/性能自检/关于 都只弹提示 | 用户会当成坏功能 |
| P6 | **注入层 vs 正式改造未决**：现在草案是"复制原页 + 注入脚本"，正式版不能靠复制 | 需要选路线（建议：共享资源 + 每页 2 行引用） |
| P7 | **51 个页面的铺开成本**：手工改不现实 | 需要幂等的批量脚本 + 逐页对比图 |
| P8 | **性能**：下拉面板 DOM 常驻、43 个 `<use>`、遮罩层 | 应改**懒挂载**（点击才建）；职业页本就是性能敏感页 |
| P9 | **可访问性**：SVG 无 `role/aria-label`；键盘只支持 ESC；深木上的浅字对比度需复核 | 无障碍与可达性 |
| P10 | **移动端未做专属布局**：目前只是把两列压成一列 | 手机上木牌过长、告示板很长 |
| P11 | **与旧页面风格割裂**：资料库/角色面板/职业页仍是米白+绿的旧风格 | 一半新一半旧会很怪 |
| P12 | **草案版本混乱**：v1~v7 目录并行，容易拿错参照 | 应冻结 v7 为唯一目标，其余标注归档 |
| P13 | **图标策略未定**：你与用户都喜欢 emoji 的辨识度，但 v7 已全面改用 SVG 描边图标 | 需要明确"混用还是统一" |

---

## 2. 更新计划（四阶段）

### 阶段 A · 逻辑修复（不涉及视觉，风险最低，先做）

| 任务 | 做法 | 验收 |
| --- | --- | --- |
| A1 上下文感知跳转 | 启动台/导航层读 `localStorage` 的**最近角色**；有角色 → 「角色面板/角色存档」带参直达；**无角色 → 置灰并提示"先创建角色"** | 无角色时不出现可点的死链 |
| A2 来源标记与返回 | 跳转带 `?from=launcher`（或 sessionStorage），目标页「返回」回到启动台 | 跳过去能原路回来 |
| A3 面板懒挂载 | 点击「工会」时才创建面板 DOM；关闭保留（二次打开更快） | 首屏 DOM 不增加 |
| A4 无角色时的替代入口 | "角色面板"位置改显示「创建新角色」 | 空状态也有正事可做 |

### 阶段 B · 自定义（P2 的直接回应）

| 任务 | 做法 |
| --- | --- |
| B1 配置结构 | `localStorage['snowd_launcher_layout'] = { v:1, board:[{type,id,label}], pinned:[] }`，带版本号 + 坏数据回退默认 |
| B2 设置页「启动台自定义」 | 从"可用入口池"勾选 + 拖拽排序；**支持固定某个角色**（读角色列表 → 生成直达该角色面板的入口） |
| B3 渲染规则 | 按配置渲染；空配置=默认布局；**上限 12 项**，超出折叠为「更多」 |
| B4 导入/导出 | 配置可导出 JSON（换机、分享） |

### 阶段 C · 风格统一（SVG 手绘组件化）

| 任务 | 做法 |
| --- | --- |
| C1 抽公共资源 | `guild-ui.css` + `guild-ui.js`（含 SVG symbol 注入 + 导航层 + 懒挂载），页面只加 **2 行引用** |
| C2 逐页替换 | 优先级：启动台 → 主页 → 角色面板 → 资料库 → 职业页首页 → 其余 46 页 |
| C3 每页对比 | 每页出"原版 vs 新版"对比图，**明显更好才提交** |
| C4 移动端专属布局 | 木牌竖排 / 告示板单列 / 下拉全屏化 |

### 阶段 D · 接线与发布

D1 设置项接线（音量 / 主题 / 自动更新 / 检查更新 / 性能自检 / 关于·许可）
D2 「关于·许可」页（含素材署名）
D3 `bump-version.js` → 构建 → NSIS + APK + OSS 镜像发布
D4 发布后回归（verify_all + 真机）

---

## 3. 里程碑判定

- **A 完成**：无角色时不再出现"点进去是空页"
- **B 完成**：用户能在设置里把"我的某个角色"固定到启动台并一键直达
- **C 完成**：至少启动台/主页/角色面板/资料库四页风格统一，且有对比图存档
- **D 完成**：设置项可用 + 新版双端发布

## 4. 风险

1. **配置兼容**：自定义配置必须带版本号，旧版读新版配置要能降级；
2. **大范围改动**：51 页统一替换风险高 → 逐页对比 + 分支开发 + 备份 zip 回滚；
3. **批量注入脚本必须幂等**，且执行前先备份。

## 5. 建议的下一步

先做 **A（逻辑修复）**：与视觉无关、风险最低、直接解决你提的 P1，并且它的结论会影响 B 的配置结构（是否需要"角色类入口"这个类型）。

---

## 6. 执行结果（2026-09-20 实施）

### 已完成

| 阶段 | 交付 | 位置 |
| --- | --- | --- |
| **A 逻辑修复** | 上下文感知跳转（读 `char_*_slot*` + 最近角色）、**无角色时置灰并提示「需先创建角色」**、`?from=launcher` 时「← 返回」改写回启动台、面板**懒挂载** | `斯诺德跑团/guild-ui.js` |
| **B 自定义** | 配置结构 `snowd_launcher_layout {v:1,counter[4],board[≤12]}`、坏数据/版本不符回退默认、**固定角色直达**（`角色面板.html?char=X&slot=N&from=launcher`）、自定义链接、导入/导出、恢复默认 | `斯诺德跑团/设置.html` |
| **C 风格统一** | `guild-ui.css` + `guild-ui.js` 共享层（自动注入样式与 24 个 SVG 符号）；**50 个页面各加 1 行引用**；启动台按 v7 重做（柜台 + 告示板 + 图钉纸条 + SVG 手绘木牌）；移动端媒体查询 | `斯诺德跑团/`、`职业页/` |
| **D 接线** | 主题/音量复用既有键（`_snowd_theme` / `_snowd_mute`）、检查更新/性能自检按桌面端能力提示、设置页「关于·许可」（含素材署名） | `设置.html` / `验证` |

### 顺带修复的真实缺陷

| 缺陷 | 表现 | 处理 |
| --- | --- | --- |
| `changelog.js` 无护栏 | `v.changes` 缺失时抛 `Cannot read properties of undefined`，**更新日志弹窗直接中断**（原版启动台也存在，非本次引入） | 加 `v.changes || []` 护栏 |
| 镜像不一致 | 改动源目录后 `electron-app/` 镜像未同步 → 龙裔同步 / 兼职规则一致性两条校验失败 | 同步 55 个文件到镜像 |

### 自验收

| 项 | 结果 |
| --- | --- |
| `node verify_guild_ui_e2e.mjs` | **27P / 0F**（A/B/D 三阶段断言：无角色置灰、直达链接参数、返回语义、懒挂载、上限 12、坏数据回退、幽灵角色、导入导出往返、主题键位） |
| `node verify_all.mjs` | 见下方「回归」小节 |
| 截图 | `_scratch/verify/v_*.png`（启动台含固定角色 / 主页面板 / 法师面板 / 设置页 / 移动端） |

### 未做（按你的要求）

- **不发版**：未改版本号、未构建、未发布。
- 图片素材未新增（本轮全部为自绘 SVG）。

---

### 第二批（2026-09-20 下午）：页面统一 + 环保合规

| 项 | 结果 |
| --- | --- |
| 共享层扩展 | `guild-ui.css` 新增 **页面外壳 / Hero / 柜台 / 木牌 / 分区标题 / 页脚 / 深色模式** 组件 + **旧页面适配器**；`guild-ui.js` 新增 `skinBoot()`（`window.__guiSkin` 开启，含标题去重） |
| 主页重做 | `斯诺德跑团/主页.html` 改为工会风格（木梁 + 英雄区 + 4 张 SVG 木牌） |
| 旧页面统一 | 8 个页面开启 `__guiSkin`（资料库/角色选择/角色存档/角色创建/上传角色/物资大全/顾问/help），由适配器统一卡片与按钮 |
| 死代码清理 | 移除 `guild-ui.js` 中重复的 `var ICONS = ''` |
| 门禁 | `verify_guild_ui_e2e.mjs` **27P/0F**；`verify_all.mjs` **Clean 54/54 · Errors 1 · Tests 890P 0F · ALL CLEAN**（1 个预存在的技能文本数据校验） |
| 镜像 | 已同步 10 个文件到 `electron-app/` |
| 环保合规（AGENTS.md） | 环境变量全部在 D 盘；**pnpm store 改道** `D:\dsh-cache\pnpm\store`（旧 C 盘 store 823 MB 已清理）；残留 `C:\Users\...\.cache\codex-runtimes` **1.3 GB 待你决定**（属 Codex 运行时，非本项目） |

---

### 第三批（2026-09-20 傍晚）：角色面板皮肤 + 三个自查发现的缺陷

| 项 | 内容 |
| --- | --- |
| 角色面板皮肤 | 页头改为**木梁**（浅色字 + 赭石描边）；内容卡片浅色=羊皮纸，**夜间=中性深色**（不硬套浅色）；战斗态保留原紫黑主题 |
| 缺陷 1（真） | 页面暗角遮罩 `z-index:0` **盖在内容之上**，把无包裹层的页面整体染色 → 改 `z-index:-1`（正确负层绘制） |
| 缺陷 2（真） | 夜间模式下面板卡片**透明**，木底透出染成橄榄色 → 改为显式双主题不透明规则 |
| 缺陷 3（我引入） | 选择器写成 `body.gui-page.gui-panel-page`，标记实际在 `<html>` 上 → 规则从未命中；**用计算样式探针发现**，非肉眼 |
| 自查手段 | 像素级 A/B：同一区域开/关遮罩截图比哈希，证明"暗角不是元凶"，从而定位到透明卡片 |
| 门禁 | guild e2e **27P/0F**；verify_all **Clean 54/54 · Errors 1 · Tests 890P 0F · ALL CLEAN**（面板相关 E2E 全绿） |

---

### 第四批（试点）：职业页 2 页工会皮肤

| 项 | 内容 |
| --- | --- |
| 试点范围 | `职业页/首页.html`、`职业页/法师.html`（其余 37 页未动，等确认） |
| 做法 | `html.gui-class-page` 作用域：顶栏→木梁（该 header 只含标题与搜索框，不含密集目录，安全）；左侧目录→羊皮纸账本；技能卡 `article.skill`→羊皮纸+木描边+hover 抬起；返回键→黄铜牌；夜间切回中性深色 |
| 首页情况 | 首页无 header / article.skill，其自带的浮雕式职业格子放在木底上反而更像木牌，未做额外改动 |
| 合规补做 | 按职业页 AGENTS.md：改过职业页 HTML 后重建搜索索引 → `node scripts/build_class_search_index.js` 写入 2734 条技能，源与 electron-app 自动一致 |
| 门禁 | 职业页懒渲染 32P/0F；移动端导航抽屉 114P/0F；工会 UI 27P/0F；verify_all Clean 54/54 · Errors 1 · Tests 890P 0F |
| 决策点 | 等确认后再批量套用其余 37 页（含 14 进阶页） |

---

### 第五批：首页图标与分区（按反馈返工）

| 项 | 内容 |
| --- | --- |
| 图标还原 | 会话中被改成的 SVG 单色徽记 → **还原为原版 emoji**（37 处，按页名逐条映射）；移除"藏 emoji"的 `font-size:0` 规则 |
| 占位符替换 | 原始就是占位符的 3 个：术士 ❓→🩸、通用天赋树 ⚪→🌳、通用·进阶 ⚪→⏫ |
| 格子实体化 | 凹槽格位 → **凸起羊皮纸牌**（顶光/底暗边/hover 抬起/active 内陷）+ emoji 圆槽 |
| 分区层级 | 三个分区改成**木牌标题栏**（含拉丁副标）；通用=黄铜描边、进阶=深木描边+顶部色带+「进」角标 |
| 自查发现的缺陷 | ① 替换样式块时**漏掉布局属性**（grid 定义与 flex 布局）导致格子塌成一行 → 已补回；② 页面自带一份 `body` 背景规则（`!important + fixed`）压过共享层 → 删除重复、背景统一由 `.gui-page` 提供；③ 全仓 10 处 `fixed` 背景 → 统一 `scroll`（长页面木纹连续） |
| 门禁 | 见下方回归结果 |

---

### 第六批：职业页全量推广

| 项 | 内容 |
| --- | --- |
| 推广范围 | 其余 **36 页**（13 基础 + 14 进阶 + 通用天赋树 + 通用·进阶 + 其余），连同已完成的首页/法师共 **38/39**；未套用 `_monk_test_top.html`（测试页，打包排除） |
| 做法 | 与试点完全一致：`window.__guiSkin + html.gui-class-page` 标记 + 共享层样式；镜像同步 36 文件 |
| 合规 | 再次重建搜索索引（2734 条技能）—— `职业页/AGENTS.md` 要求 |
| 抽查 | 战士（119 卡）/ 战士·进阶 / 通用天赋树（400 卡）/ 召唤师（47 卡）：木梁顶栏 ✓ 目录纸 ✓ 工会按钮 ✓ 背景连续 ✓ 0 报错 |
| 门禁 | 见下方回归结果 |
