# 文件整理规范

> 制定：2026-09-20 ｜ 适用范围：本仓库全部目录（根目录 + 子项目）
> 性质：**强制约定**。人工与 AI 助手每次新增、删除、移动文件都按本规范执行；与其他文档冲突时以本规范的文件落位部分为准。

---

## 0. 一句话规则

**根目录只放「数据源 + 规范文档 + 发布链路 + 验证门禁」；一切临时脚本与产物进 `_scratch/`；任务收尾或发版前按月份归档到 `备份区/YYYY-MM/`。**

---

## 1. 根目录白名单（只有这些可以留在根目录）

| 类别 | 内容 | 说明 |
| --- | --- | --- |
| 数据源（地面真值） | `基础职业-*.docx`、`通用天赋树.docx`、`特殊专长.docx`、`神圣领域-*.docx`、`已公布进阶职业.docx`、`《基础职业进阶途径》.docx`、`斯诺德世界观架构.docx`、`斯诺德核心文档.rar`、`冒险者*.xlsx`、`个性与背景创建规则.xlsx`、`斯诺德*.xlsx/xls`、`*_角色档案.xlsx`、`兼职条件.png`、`兼职需求(谋士版本).png`、`额外规则.png`、`龙裔种族详情.png` | **禁止移动**：docx/xlsx/图片是全部管线的比对基准 |
| 规范与知识文档 | `项目文档.md`、`工作规则.md`、`后续更新教程.md`、`职业页同步手册.md`、`文件整理规范.md`、`关键词系统注意事项.md`、`卡牌效果实现进度.md`、`通用天赋树.md`、`debug-journal.md` | 长期维护，改版时同步更新 |
| 发布链路 | `bump-version.js`、`changelog.js`、`package.json`、`package-lock.json`、`index.html`、`server.js`、`sentry-loader.js`、`bug-report.js`、`更新打包.bat`、`upload-gitee-exe.sh`、`opencode.json`、`.gitignore`、`.env`/`.env.example` | `index.html` 被 `deploy.yml`/`deploy-oss.yml` 引用，勿动 |
| 验证门禁 | `verify_all.mjs`、`verify_*.mjs`、`verify_*.cjs`、`verify_e2e.js`、`describe_screenshot.mjs` | **必须留在根目录**：`verify_all.mjs` 以根相对路径 spawn 它们；若要迁入 `scripts/`，必须同步改 `verify_all.mjs` 与 `工作规则.md`/`职业页同步手册.md` |
| 目录 | `斯诺德跑团/`、`职业页/`、`electron-app/`、`scripts/`、`mobile/`、`poker-game-main/`、`snode-combat/`、`advisor*/`、`docs/`、`music/`、`.github/`、`备份区/`、`tmp/`、`screenshots/` | 既有目录结构不变 |

**根目录禁止出现**：`_*` 前缀文件、`_shot_*`、`_tmp_*`、`_smoke_*`、`_dbg_*`、一次性 HTML 展示页、本地安装包（`*.exe`）、重复归档（`*.zip`、`*.tgz`）。

---

## 2. 临时脚本与产物放哪

| 类型 | 位置 | 备注 |
| --- | --- | --- |
| 一次性脚本（探针 / 调试 / 批量修数据 / 冒烟测试） | `_scratch/` | 不入库；命名建议 `动作_对象.mjs\|py`，如 `diff_summoner_docx.py` |
| 脚本产物（txt / json / log / 中间 HTML） | `_scratch/` | 同上 |
| 截图 / 视觉检查图 | `screenshots/` | 已 gitignore；`verify_visual.mjs` 已按此执行 |
| docx / xlsx 解析中间产物 | `_scratch/` | 旧 `_docx_tmp/` 已整体归档，新工作请用 `_scratch/` |
| 长期复用工具（会被反复调用的脚本） | `scripts/` | 入库、可复用、带说明；例：`scripts/archive_files.mjs` |
| 页面级产物（职业页 HTML/JSON、skill_effects 等） | 对应子项目目录 | 见《职业页同步手册》 |
| 子项目设计文档（战斗系统 v2 等） | 该子项目 `docs/` | 例：`snode-combat/docs/战斗系统设计v2.md`（2026-09-20 由根目录迁入） |

---

## 3. 备份区结构与归档规则

```
备份区/
├── _归档索引.md          # 每次归档自动刷新：分档规则 + 全量清单 + 统计
├── 2026-02/ … 2026-09/   # 时间分档（按月份）
└── <后续月份>/
```

- **分档粒度**：按月（`YYYY-MM`），保证「同一大时间段的临时文件与脚本在同一文件夹内」，且目录名可直接排序。
- **月份判定优先级**：① 文件名中的日期（`2026-07-31_xxx`、`xxx_20260529_225438`、`xxx-20260818-021546`）→ ② 目录内**最新**文件的修改时间 → ③ 条目自身修改时间。
- **同名冲突**：追加 `__dup2`、`__dup3` …… **绝不覆盖**。
- **绝不删除**：归档只做移动；确认无引用后才允许删除（删除前先 `git status` 与全仓 grep 引用）。
- **仓库跟踪文件**：若被归档条目是 git 跟踪文件，`git status` 会显示删除 —— 必须逐条确认「确实无引用」后再连同提交一起落地，否则 `git checkout -- <路径>` 回退。

### 归档命令

```bash
node scripts/archive_files.mjs            # 预演（dry-run）：打印全部计划，不落盘
node scripts/archive_files.mjs --apply    # 执行归档 + 刷新 备份区/_归档索引.md
```

---

## 4. 每次删改文件的执行清单（强制）

1. **新建文件前**先判断归属：数据源 / 规范文档 / 发布链路 / 门禁 → 按 §1 落位；否则 → `_scratch/`（截图 → `screenshots/`）。
2. **任务收尾或发版前**执行 `node scripts/archive_files.mjs` 预演，确认清单无核心文件后 `--apply`。
3. **归档后立刻** `git status` 复核：出现非预期的跟踪文件删除，立即 `git checkout -- <路径>` 回退并向用户说明。
4. **不要**把临时脚本提交进仓库；需要长期复用的，再迁到 `scripts/` 并在此文档登记。
5. **发版前**检查根目录是否又出现 `_*/临时产物`：有则先归档再 bump。
6. 新增/变更落位规则时，更新本文档 §1/§2 与 §5 变更记录，并同步 `_scratch/README.md`。

---

## 5. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-20 | 第二批：内容型杂项归档 8 项（`斗蛐蛐记录*` 6 个、`narrative_log.md`、`Bucolic Dream…txt`）→ `备份区/2026-07`、`2026-08`；`战斗系统设计v2.md`/`战斗系统AI指导v2.md` 迁入 `snode-combat/docs/` 并同步 3 处引用 |
| 2026-09-20 | 归档工具增强：新增指定条目模式 `--paths-file <清单> [--why <原因>]`；索引改为**按批次追加**（保留历史清单）；修正紧凑日期正则，避免长数字串（如 `Av117020930606041`）被误判为日期 |
| 2026-09-20 | 首次制定。根目录归档 186 项（`_shot_*`/`_tmp_*`/`_smoke_*`/`_dbg_*`、本地安装包、测试导出 xlsx、游离副本等）；备份区顶层 301 项按月份归位为 `2026-02`…`2026-09`；新增归档工具 `scripts/archive_files.mjs` 与临时工作区 `_scratch/` |
