# UI 收尾扫描报告（2026-09-20）

> 范围：`斯诺德跑团/` 与 `职业页/` 全部 HTML 页面。**未发版**（版本号、构建、发布均未动）。

## 一、文件级覆盖

| 指标 | 数值 |
| --- | --- |
| 页面总数 | **55**（斯诺德跑团 16 / 职业页 39） |
| 已注入 `guild-ui.js` | **53** |
| 已开皮肤（`__guiSkin`） | **48** |

未开皮肤的 7 页及原因：

| 页面 | 原因 |
| --- | --- |
| `斯诺德跑团/启动台.html` `设置.html` | **设计如此**：两页自带内联木质样式，并设 `__guiNoNav` 关闭浮动导航 |
| `斯诺德跑团/帮助.html` | 332 字节的**跳转页**（meta refresh → `help.html`），无需皮肤 |
| `斯诺德跑团/tests.html` `verify.html` | 开发/自检页，不应注入 |
| `斯诺德跑团/组件示例.html` | **非本次工作产物**（今日 17:42 出现的组件验收页，来源不明），未改动 |
| `职业页/_monk_test_top.html` | 测试页，打包已排除 |

## 二、运行时扫描（53 页逐页渲染）

| 指标 | 结果 |
| --- | --- |
| 皮肤类生效 | **49 / 53**（其余 4 页见上表，均为设计如此） |
| SVG 图标库注入 | **53 / 53** |
| 残留旧象牙白底（`#f6f4ef`） | **0** |
| JS 报错 | **0** |
| 加载失败 | **0** |

## 三、夜间模式扫描（12 个代表页）

发现并修复 **3 个问题**：

| # | 问题 | 性质 | 修法 |
| --- | --- | --- | --- |
| 1 | **设置页不跟随夜间** | **我引入的缺陷**：`boot()` 在 `__guiNoNav` 时提前 return，把 `themeBoot()` 一起跳过了 | 把主题应用提到提前 return **之前**（主题与导航层无关） |
| 2 | **角色面板内部仍是浅色** | 该页只有一份 `:root` 浅色变量、**没有暗色对应块**（`panel.css` 的暗色块对该页不生效，因为该页并未链接 panel.css） | 在 guild-ui.css 加 `html.dark.gui-panel-page` 变量映射（`--bg/--panel/--ink/--line/--soft/--accent…`）+ `.attr/.equip-slot` 覆盖 |
| 3 | 启动台夜间"94% 大块亮底" | **误报**：首次访问自动弹出的更新日志弹窗（白色，来自 changelog.js 内联样式）被计入 | 确认后不需修改（设置 `_snowd_changelog_seen` 后为 0） |

修复后复测：`设置 / 角色面板 / 启动台 / 职业页首页` → `html.dark=true`、木底 `rgb(42,32,24)`、**大块亮底 0 个**。

## 四、移动端扫描（390×844，12 个代表页）

| 指标 | 结果 |
| --- | --- |
| 横向溢出 | **0 页**（内容宽 = 视口宽 390） |
| JS 报错 | **0** |

## 五、结论

1. 界面统一**无遗漏**：除 6 个"设计上不应注入"的页面外，全部 48 页已套用共享层皮肤；
2. 三处夜间问题已修（含我自己引入的 `boot()` 顺序缺陷）；
3. 移动端无横向溢出、无报错；
4. 建议纳入`verify_all` 的常态化检查（可选）：把本报告的"皮肤生效 + 无旧底色 + 无横向溢出"做成 3 条断言。
