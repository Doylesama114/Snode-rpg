# 更新计划 · 「表头 + 短内容居中」全站推广

> 本文只做盘点与计划，**未改动代码**（help.html 内的本地试改保持原样，待本计划落地时收敛到共享层）。
> 目标：把已确认的效果（**表头居中、短内容居中、长段落左对齐**）推广到**所有涉及表格的位置**。

---

## 一、盘点结果（只读实测）

站内"表格"其实有 **3 类载体**，不能只改 `<table>`：

| # | 载体 | 数量 / 分布 | 说明 |
| --- | --- | --- | --- |
| 1 | **静态 `<table>`** | **46 张**，6 个页面：help.html(36)、角色面板(6)、物资大全(1)、组件示例(1)、资料库(1)、通用·进阶(1) | 规则手册与面板为主 |
| 2 | **JS 生成 / 懒渲染的表格** | 资料库（`tableBox` ×3，创建流程/物资大全）、角色面板、物资大全、通用·进阶 | 初始化时不存在 ✗ → 一次性遍历会漏 |
| 3 | **div 网格型"表格"** | **`.attr-table` 共 2803 处，20 个职业页**（技能卡详情：施展时间/距离/持续时间…）| **不是 `table` 标签** ✗，当前样式改动完全覆盖不到 |

补充：角色面板 24 处 `display:grid`、资料库 4 处、创建页 10 处 —— 其中部分是**布局 grid**（不能碰 ✗），部分是**表格式数据**（可居中 ✓），需要按**类名白名单**区分。

---

## 二、统一方案（一处改，全站生效）

### 2.1 共享层 CSS（改 `guild-ui.css` —— 所有页面都会注入 ✓）
```css
/* 表格类：表头与单元格默认居中 */
.wrap table th, .wrap table td,
.rules-table th, .rules-table td,
.attr-table .attr-name, .attr-table .attr-val,
.data-table th, .data-table td { text-align: center; }
/* 长段落豁免（由 JS 打标） */
.tl, .tl * { text-align: left !important; }
/* 需要保持左对齐的显式类（如说明列） */
.keep-left, .keep-left * { text-align: left !important; }
```

### 2.2 共享层 JS（改 `guild-ui.js`）
- 遍历上述 3 类载体的单元格，**文本长度 > 24 字**（或含块级子元素）→ 加 `.tl` ✓
- **MutationObserver** 监听 DOM 变化（懒渲染技能卡、资料库动态表、面板重渲染）→ **节流 200ms** 后增量处理 ✓
- 暴露 `SnowdGuild.centerTables()` 便于页面主动调用与测试 ✓

### 2.3 收敛临时改动
- 删除我加在 `help.html` 的内联 `<style>/<script>`（已由共享层覆盖 ✓），避免两处逻辑打架 ✗

---

## 三、分组落地与例外

| 分组 | 处理 |
| --- | --- |
| **A 规则手册（help.html）** | 沿用已确认效果 ✓（46 张表中 36 张） |
| **B 技能卡 div 网格（2803 处）** | `.attr-name`（字段名）居中；`.attr-val`（值）**默认居中**，若值本身是长句（>24 字）→ 左对齐 ✓ |
| **C 资料库 / 物资大全（xlsx 表格）** | 表头居中；**长文本列左对齐**（xlsx 表格以段落为主 ✗ 不宜居中） |
| **D 角色面板（属性/装备/技能表）** | 短值居中 ✓（数字列尤其受益）；`display:grid` **布局**容器不动 ✓ |
| **E 角色创建页 / 角色选择页 / 上传角色 / 顾问** | 仅对显式表格式类名生效 ✓（避免误伤布局） |
| **F 移动端** | 同一套规则 ✓；390px 下复核首屏与长文换行 ✓ |

---

## 四、实施步骤

1. 共享层 CSS + JS（`guild-ui.css` / `guild-ui.js`）→ 镜像同步 ✓
2. 删除 `help.html` 的临时内联块 ✓
3. 逐分组抽查（A→F），逐页截图核对 ✓
4. 新增验收脚本 `verify_table_center.mjs`：
   - 抽样断言：短内容 `text-align: center` ✓ / 长内容 `left` ✓ / 表头 `center` ✓
   - **动态场景断言**：滚动触发技能卡懒渲染后，新插入的 `.attr-val` 也满足上述规则 ✓（验证 MutationObserver 生效 ✓）
   - 反例断言：布局 grid（如 `.equip-grid`）**未被**误改 ✓
5. 全量门禁 `verify_all` + 移动端脚本回归 ✓
6. **发版 v1.0.8014**（发版前逐页截图复验 ✓）

---

## 五、风险与对策

| 风险 | 对策 |
| --- | --- |
| **懒渲染 / 动态插入**导致漏改 | MutationObserver + 节流；测试断言"滚动后生效" ✓ |
| 误伤**布局 grid**（面板/创建页） | 只对**类名白名单**生效（`.attr-table/.wrap table/.data-table/.kvRow` 等）；反例断言 ✓ |
| 长句被居中后难读（名望「要点」等） | 24 字阈值豁免 ✓（可按反馈调成 16/40 ✓） |
| 数字列居中后**对齐感**变化 | 面板数字列本就应居中 ✓；截图核对 ✓ |
| `!important` 冲突 | 共享层规则用**低侵入**写法；豁免用 `.tl` 单一入口 ✓ |
| 暗色模式 | 仅对齐属性，不影响配色 ✓，仍跑暗色截图 ✓ |

---

## 六、待你确认（默认按此执行）

1. **阈值 24 字**是否合适？（决定多少单元格参与居中）
2. **技能卡的字段值**（如"施展距离：6米"）：**值居中** ✓ 还是**保持左对齐、只让字段名居中**？
3. 面板的**数字/短值列**（属性值、等级、价格）是否也一并居中 ✓（默认：是）
