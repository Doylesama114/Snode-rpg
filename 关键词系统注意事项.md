# 关键词筛选系统实现注意事项

## 1. 技能 HTML 中的关键词标签格式

每个技能卡片内的关键词标签必须使用以下格式：

```html
<div class="chips">
  <span class="chip">战技</span>
  <span class="chip">近战攻击</span>
</div>
```

**关键点：**
- 使用 `class="chip"`（不要加额外 class，不需要 `kw-chip`）
- 不需要 `data-kw` 属性
- 法师兼容使用 `class="chip kw-chip" data-kw="..."` 格式，但其他职业统一用简洁的 `class="chip"` 格式

## 2. 事件绑定方式：使用直接绑定

```javascript
// ✅ 正确方式：直接绑定在每个 chip 元素上
document.querySelectorAll("#view-xxx .chips .chip").forEach(function(chip) {
    chip.addEventListener("click", function(e) {
        // 处理关键词点击
        e.stopPropagation();
    });
});

// ❌ 避免使用：文档级事件委托（容易与其他事件处理器冲突）
document.addEventListener("click", function(e) {
    // 这种方式会被多个类各自注册的事件处理器干扰
});
```

## 3. 每个职业使用独立的作用域和前缀

```javascript
// 使用 IIFE 隔离变量，避免命名冲突
(function() {
    var x_filterBar = document.querySelector("#view-xxx #x-filter-bar");
    var x_activeFilters = new Set();
    // ...
})();
```

变量前缀对照表：

| 职业 | HTML view ID | JS 前缀 | viewId 参数 |
|------|-------------|---------|-------------|
| 战士 | view-warrior | w_ | warrior |
| 法师 | view-mage | (独立实现) | mage |
| 游荡者 | view-r | r_ | r |
| 吟游诗人 | view-bard | b_ | bard |
| 蛮斗士 | view-berserker | be_ | berserker |
| 猎人 | view-hunter | h_ | hunter |
| 圣骑士 | view-paladin | pa_ | paladin |

## 4. 筛选栏 HTML 结构

```html
<div class="filter-bar" id="{prefix}-filter-bar"></div>
```
初始为空容器，由 JS 动态填充。

## 5. JS 脚本标签必须干净

- 不能有 BOM 字符（`\uFEFF`）在 `<script>` 标签内
- 不能有空的 `<script>` 标签或多余的不闭合标签
- 每个 JS 块要确保花括号和括号平衡（`(`/`)`、`{`/`}` 数量匹配）

## 6. HTML 结构必须完整

每个职业页面必须包含以下元素：

- `#x-filter-bar` — 关键词标签展示区域（在导航菜单内）
- `#x-search` — 搜索输入框（在顶部栏）
- `#x-empty` — 无匹配内容提示（在内容区起始处）
- 每个技能要有 `<div class="chips">` 包含 `<span class="chip">关键词</span>`

## 7. 筛选逻辑要点

- **视图隔离**：通过 `while(parent)` 向上遍历检查 `parent.id === "view-{viewId}"`，确保点击只触发在当前职业视图内
- **OR 筛选逻辑**：选中多个关键词时，技能只需包含任一关键词即显示
- **chip 内容过滤**：跳过内容包含"风格"或"天赋树"的 chip（它们是标题标签，不是可点击关键词）
- **目录联动**：筛选时同步隐藏目录导航中无匹配链接的条目，展开有匹配的目录分组
- **搜索独立**：搜索时清除关键词筛选状态
- **空结果提示**：无匹配技能时显示 empty 中的提示文本

## 8. CSS 依赖

以下 CSS 类需要已定义（全局样式）：

| 类名 | 作用 |
|------|------|
| .filter-hidden | display: none — 隐藏元素 |
| .filter-active | 绿色高亮（background: var(--green); color: #fff） |
| .filter-inactive | 半透明淡化（opacity: 0.4） |
| .filter-tag | 筛选栏标签样式 |
| .filter-tag .remove | × 移除按钮 |
| .hidden | display: none — 通用隐藏 |

## 9. 其他注意事项

- **费用行不能独立成技能**：费用行（如`费用: ●●○`）必须合并到所属技能中，作为技能描述的一部分
- **样式标签过滤**：`<span class="chip">惩戒风格</span>` 这类样式标签虽然有 `.chip` 类，但要通过内容判定确保不被当作可点击关键词处理
- **搜索功能依赖 `data-search`**：搜索框的 `data-search` 属性要包含技能名称、风格、阶位、关键词、前置条件等所有可搜索信息
- **费用颜色**：从 docx 的字体颜色提取，使用 `<span style="font-size:1.5em;color:#FF0000">●</span>` 格式渲染
- **关键词提取**：从每个技能的"关键词：A.B.C" 字段提取，生成关键字芯片

---

## 10. 斯诺德对决 · 卡组构筑筛选（v1.0.6101+）

与职业技能页的筛选**不是同一套逻辑**，实现于 `poker-game-main/`：

| 维度 | 逻辑 | 说明 |
|------|------|------|
| 类别 / 属性 | 多选 **OR** | 满足所选其一即可 |
| 关键词 | 多选 **AND** | 卡牌须同时包含全部已选关键词 |
| 跨维度 | **AND** | 类别 + 属性 + 关键词 + 搜索词叠加 |
| 搜索框 | 模糊匹配 | 名称、关键词、属性、**效果描述** |
| 排序 | 单字段 | 费用/战力排序时 **零战力（战术等）沉底** |

**UI**：右侧 **悬浮筛选窗**（`position: fixed`，滚动页面时仍可用），可收起为边缘「筛选」标签或展开为面板；展开时可能遮挡部分卡池，收起即可查看。组件：`DeckPoolFilterSidebar.vue`（Teleport 至 body）。

**勿混淆**：职业页关键词筛选为 OR；卡组关键词筛选为 AND（找「武器+居民」等组合标签）。
