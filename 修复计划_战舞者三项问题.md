# 修复计划 · 战舞者三项问题（关键词丢失 / 内容缺失 / 首页进阶入口）

> 本文只做诊断与计划，**未改动代码**。三处均已用数据坐实。

---

## 一、问题 1 · 「天赋」等类型关键词未被筛选识别（**全局性问题，不止战舞者**）

### 证据
| 检查 | 结果 |
| --- | --- |
| 战舞者 15 条「天赋」关键词技能 | 页面 `data-type="天赋"` ✓ **但 `data-tags` 里没有「天赋」** ✗ |
| 页面 `data-tags` 内容样例 | `连环奇袭 → tags[连续攻击,每轮限一次]` ✗（只有行为关键词） |
| 对照法师 | `data-search` 含类型词 **423/423** ✓，但 `data-tags` 含「法术」仅 **16/423** ✗ |
| **全站审计（16 职业 + 通用天赋树）** | **2125 条技能**的 docx 关键词未进入站点 `tags` ✗（例：法师 405、吟游诗人 152、魔契师 141、战士 119、武僧 127、战舞者 53 …） |
| 筛选面板读取字段 | `filter-panel.js` / `filter.js` 均只读 **`data-tags`** 与「关键词」文本 ✗ **不读 `data-type`** |

### 根因
`scripts/class_sync_core.py` 的 `tags_from_keywords()` 只保留"行为关键词"，把**类型类关键词**（法术 / 战技 / 天赋 / 戏法 / 短休 / 长休 …）**过滤掉** ✗。而这些词恰好是玩家最常用的筛选项 ✗。

> 结论：**任何职业都无法按「天赋/法术/战技」筛选** ✗ —— 只是在战舞者上被注意到。

### 修法（建议两层一起做）
1. **短修（全局生效、改动小）**：滤镜关键词池改为 **`data-tags` ∪ `data-type`**（同时纳入 `data-tier`/`data-style` 已有 ✓）→ 立刻可按类型筛选 ✓
2. **长修（数据补齐）**：生成器 `tags_from_keywords` 保留类型关键词；写一个**一次性注入脚本**给既有 19 个职业页的 `data-tags` 补上类型词（幂等 ✓）
3. **验证**：新增断言 —— 任取 ≥5 个职业，逐技能比对 docx `关键词` 与页面（`data-tags` ∪ `data-type`），**缺失数必须为 0** ✓

---

## 二、问题 2 · 「无迹风舞」内容缺失（提取器启发式缺陷）

### 证据
| 位置 | 内容 |
| --- | --- |
| 站点 JSON | `fields.描述` = "你的每一步都精准地落在敌人攻击范围的间隙中…" ✓；`description` = **["持续时间内你获得以下效果："]** ✗ —— **后续要点全部丢失** ✗ |
| docx 提取产物 | 与站点**完全一致**（同样缺要点）✗ → 说明**丢失发生在提取阶段**，不是落站阶段 |
| 对应线索 | M0 记录的「具名无卡 4 项」正是这些要点：`·基础移动速度+2米`、`·敏捷或魅力属性检定`、`·结算前的伤害值点数`、`3.你至多拥有三颗沸血骰`（后者属**沸血之舞**） |

### 根因
`extract_class_docx.py` 的名字启发式把**以「·」/「3.」开头的要点行误判为"技能名"** ✗ → 既没进 description，也没生成卡片（M0 已记为"具名无卡"✗）。

### 修法
1. **修提取器**：位于"以「：」结尾的引导句"之后的 `·` / `数字.` 行 → 归入该技能的 `description`（不当作技能名）✓
2. **重新提取 → 重建战舞者数据与页面**（技能数仍 54 ✓，内容补齐 ✓）
3. **全站同类扫描**：以"description 首项以「：」结尾且后续要点数 < docx 要点数"为判据逐职业核对（初筛已发现 1 条疑似：游荡者 `窃贼的交易` → 一并核查）✓
4. **验证**：新增断言 —— 每个技能 `description` 的要点条数 ≥ docx 中该技能段落的要点条数 ✓

---

## 三、问题 3 · 首页缺「战舞者·进阶」入口（明确、简单）

### 证据
| 检查 | 结果 |
| --- | --- |
| `职业页/·进阶.html` 文件数 | **19** 个 ✓ |
| 首页已链接的进阶页 | **18** 个 ✗ |
| **未链接** | **`战舞者·进阶.html`** ✗（唯一一个） |
| 首页分区 | 基础职业 / 通用 / **进阶途径** ✓（进阶区存在，只是漏了这条） |

### 修法
在首页「进阶途径」区**按现有卡片格式补一条**（emoji + 「战舞者进阶」+ `href="战舞者·进阶.html"`）✓ → 同步 electron 镜像 ✓
**验证**：断言「首页链接的进阶页数 == 进阶页文件数（19）」✓

---

## 四、执行顺序与验证

| 步骤 | 内容 | 验证 |
| --- | --- | --- |
| 1 | **问题 3**（1 处卡片） | 首页进阶链接数 19/19 |
| 2 | **问题 1 短修**（滤镜 ∪ data-type） | 手动筛「天赋」能命中 15 条 ✓ |
| 3 | **问题 1 长修**（注入脚本补 data-tags） | 全站关键词缺失 = 0 |
| 4 | **问题 2**（修提取器 → 重建战舞者 → 全站同类核对） | 无迹风舞要点齐全 + 全站要点数断言 |
| 5 | 回归 + 发版 | `verify_all` Clean · 关键词/要点新断言 · 截图核验 · tag `v1.0.8012` |

---

## 五、风险与注意

| 风险 | 对策 |
| --- | --- |
| 问题 1 的滤镜改动可能影响现有筛选行为 | 只**增加**关键词来源（并集），不改变原 data-tags 逻辑 ✓；改完跑 `verify_class_lazy_e2e` / 专长切换 e2e ✓ |
| 注入 data-tags 涉及 19 个页面（含超大单行 ✗） | 用脚本按 `data-tags="…"` 正则逐卡注入（幂等 ✓），**不用 edit 工具碰超大行** ✓ |
| 改动提取器可能影响**其他职业**的提取结果 | 先只重跑战舞者并 diff；确认其他职业提取产物**逐字节不变**后再考虑全量 ✓ |
| 问题 2 的重建会改 `战舞者.json/html` | 重跑生成器 + 懒渲染转换 + 搜索索引重建（2734+54 保持 ✓）+ 镜像同步 ✓ |
