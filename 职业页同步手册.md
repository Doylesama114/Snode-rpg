# 职业页同步手册（26.06.30 规则）

> 本文档记录：**docx 源文件 → 职业页 HTML + 数据 JSON + skill_effects JSON** 的同步进度、标准步骤与注意事项。  
> 每完成一个职业（或模块），**必须更新本文档的进度表**（见 [Step 9](#step-9更新本手册进度)）。

**最后更新**：2026-07-09  
**规则版本**：26.06.30  
**已完成职业**：战士、吟游诗人、法师、游荡者、术士、圣骑士、萨满祭司、猎人、牧师、德鲁伊、武僧、蛮斗士、魔契师  
**进阶途径**：✅ 已从 `《基础职业进阶途径》.docx` 全量同步（v1.0.7089+）

---

## 一、规则变更摘要

| 项目 | 旧 | 新（26.06.30） |
|------|----|----------------|
| 字段标签 | `费用：` | **`标识：`** |
| 彩色 ● 含义 | 消耗技能点 | **色彩标识**（团队结算获得，学习技能需持有对应颜色） |
| 技能点 | 彩色 SP 消耗 | **数值型**（0–2/次，1 点学 1 个技能/天赋） |
| docx 中 ● | 费用 | **标识**（颜色要求，非个人消耗） |

**不在本次范围**（除非另行开启）：

- `帮助.html` 全文改写
- **GM 团队色彩结算 UI**（见下方待办；v1.0.6118 已实现面板侧学习校验，不实现各技能「游玩结束授予标识」自动化）

**进阶途径（26.07.09 已纳入）**：

- 源文件：`《基础职业进阶途径》.docx`（根目录）
- 输出：`职业页/*·进阶.html`、`职业页/数据/*·进阶.json`、`职业页/advancement_data.json`
- Advisor：`advisor/advancements.json`（281 条去重）、`advisor/advancement_skills.json`（`advancement_details.js` documented 档）
- 脚本：`scripts/advancement_sync_core.py` + `scripts/apply_advancement_sync.py`

**v1.0.6118 已完成（角色面板）**：

- `panel_engine.js`：`sp_points` + `color_marks`；固定色 / 无色 / 炫彩 wildcard 学习校验；1 SP 学 1 技能；卸载退 1 SP
- 移除旧 14 色 SP 弹窗与 `state.sp` 迁移逻辑
- GM 作弊面板可手动调整 SP 与标识（临时替代结算 UI）

**待办（DM 功能）**：

- 团队奖励结算页：扫描角色已学天赋/专长/技能 → 生成「谁该获得哪些标识/技能点」清单 → DM 确认后批量写入 `color_marks` / `sp_points`
- 不实现各技能文案中的战斗/社交/跨角色 SP 效果（辅助软件定位，桌面团 DM 处理）

---

## 二、涉及文件

### 每个基础职业

| 类型 | 路径 |
|------|------|
| docx 源 | `基础职业-{职业名}.docx` |
| HTML | `职业页/{职业名}.html` |
| 数据 JSON | `职业页/数据/{职业名}.json` |
| 效果 JSON | `斯诺德跑团/skill_effects_{职业名}.json`（可能不存在，见 [§七](#七首次创建-skill_effects-文件无现成文件时)） |
| electron 镜像 | `electron-app/职业页/` 与 `electron-app/斯诺德跑团/` 下同名文件 |

### 额外模块

| 模块 | docx | HTML | JSON |
|------|------|------|------|
| 通用天赋树 | `通用天赋树.docx` | `职业页/通用天赋树.html` | `职业页/数据/通用天赋树.json` |
| 特殊专长 | `特殊专长.docx` | `职业页/特殊专长.html` | `职业页/数据/特殊专长.json` |

### 参考文档

- `.opencode/skills/sync-class-from-docx/SKILL.md` — 完整六阶段协议（Phase 1–6）
- `斯诺德跑团/skill_effects_schema.md` — 效果 JSON 编写规范

### 脚本

| 脚本 | 用途 |
|------|------|
| `scripts/class_sync_core.py` | **通用** docx 解析 + HTML/JSON/FX 同步核心库 |
| `scripts/feat_sync_core.py` | 特殊专长 docx 解析 + HTML/JSON/FX（**非** class_sync_core） |
| `scripts/apply_warrior_sync.py` | 战士专用入口（旧，可改用 core） |
| `scripts/apply_bard_sync.py` | 吟游诗人专用入口 |
| `scripts/apply_mage_sync.py` | 法师专用入口 |
| `scripts/apply_rogue_sync.py` | 游荡者专用入口 |
| `scripts/apply_warlock_sync.py` | 术士专用入口 |
| `scripts/apply_paladin_sync.py` | 圣骑士专用入口 |
| `scripts/rebuild_shaman_from_docx.py` | 萨满祭司：从 docx **重建** HTML/JSON/FX |
| `scripts/apply_shaman_sync.py` | 萨满祭司增量同步入口 |
| `scripts/apply_hunter_sync.py` | 猎人专用入口 |
| `scripts/apply_cleric_sync.py` | 牧师专用入口 |
| `scripts/apply_druid_sync.py` | 德鲁伊专用入口 |
| `scripts/apply_monk_sync.py` | 武僧专用入口 |
| `scripts/apply_barbarian_sync.py` | 蛮斗士专用入口 |
| `scripts/apply_pactmaker_sync.py` | 魔契师专用入口（勿与 `apply_warlock_sync.py` 术士混淆） |
| `scripts/apply_general_talents_sync.py` | 通用天赋树专用入口 |
| `scripts/apply_special_feats_sync.py` | 特殊专长专用入口（`feat_sync_core.py`） |
| `scripts/advancement_sync_core.py` | **进阶途径** docx → HTML/JSON/advancement_data + Advisor 索引 |
| `scripts/apply_advancement_sync.py` | 进阶途径一键同步（含 `build-advisor-advancements.mjs`） |
| `scripts/build-advisor-advancements.mjs` | 从 15 个 `*·进阶.json` 重建 `advisor/advancements.json` |
| `scripts/build-advisor-advancement-skills.mjs` | 从 `advancement_details.js` 重建 `advancement_skills.json` |
| `scripts/add_druid_missing_json.py` | 德鲁伊：补全 JSON 缺失的 `d-skill-5` 荆棘术 |
| `scripts/add_monk_missing_json.py` | 武僧：补全 JSON 缺失的 17 条（`mo-skill-115`~`131`） |
| `scripts/add_rogue_missing_json.py` | 游荡者：补全 JSON 缺失的四阶等 25 条 |
| `scripts/add_warlock_missing_json.py` | 术士：补全 JSON 缺失的四阶 4 条（语义 id） |
| `scripts/fill_warrior_fx_gaps.py` | 战士：修正 skill_effects ID 漂移 |
| `scripts/verify_warrior_sync.py` | 战士验证 |
| `scripts/verify_bard_sync.py` | 吟游诗人验证 |
| `scripts/verify_mage_sync.py` | 法师验证 |
| `scripts/verify_rogue_sync.py` | 游荡者验证 |
| `scripts/verify_warlock_sync.py` | 术士验证 |
| `scripts/verify_paladin_sync.py` | 圣骑士验证 |
| `scripts/verify_shaman_sync.py` | 萨满祭司验证 |
| `scripts/verify_hunter_sync.py` | 猎人验证 |
| `scripts/verify_cleric_sync.py` | 牧师验证 |
| `scripts/verify_druid_sync.py` | 德鲁伊验证 |
| `scripts/verify_monk_sync.py` | 武僧验证 |
| `scripts/verify_barbarian_sync.py` | 蛮斗士验证 |
| `scripts/verify_pactmaker_sync.py` | 魔契师验证 |
| `scripts/verify_general_talents_sync.py` | 通用天赋树验证 |
| `scripts/verify_special_feats_sync.py` | 特殊专长验证 |

> 新职业：复制 `apply_bard_sync.py` 改类名与路径，或调用 `class_sync_core.sync_class()`。

---

## 三、更新进度

状态图例：`✅ 已完成` · `🔄 进行中` · `⬜ 未开始` · `— 无 fx 文件`

### 3.1 十四基础职业

| 职业 | docx | HTML `费用→标识` | 数据 JSON | skill_effects | 验证 | 备注 |
|------|:----:|:----------------:|:---------:|:-------------:|:----:|------|
| 战士 | ✅ | ✅（0 处 `费用`） | ✅ 109 技能 | ✅ 109/109 | ✅ | **试点完成**；曾修正 ID 漂移与龙卷风射击 |
| 法师 | ✅ | ✅（0 处 `费用`） | ✅ 356 技能 | ✅ 356/356 | ✅ | 8 起始；8 学派；docx 已删 `虹光射线` 同步时移除；52 组重名靠 style 匹配 |
| 牧师 | ✅ | ✅（0 处 `费用`） | ✅ 54 技能 | ✅ 54/54 **新建** | ✅ | 4 起始；3 风格（戒律/虔佑/魂谒）；15 组重名 |
| 圣骑士 | ✅ | ✅（0 处 `费用`） | ✅ 58 技能 | ✅ 58/58 | ✅ | 5 起始；4 风格（惩戒/守护/圣洁/热诚）；21 组重名 |
| 萨满祭司 | ✅ | ✅（0 处 `费用`） | ✅ 98 技能 | ✅ 98/98 | ✅ | 4 起始；5 风格；**自 docx 重建**（原 HTML 损坏）；docx 删 4 技能 |
| 术士 | ✅ | ✅（0 处 `费用`） | ✅ 48 技能 | ✅ 48/48 | ✅ | 3 起始；潜能单风格；JSON 原缺 4 条四阶（语义 id）已补全 |
| 德鲁伊 | ✅ | ✅（0 处 `费用`） | ✅ 77 技能 | ✅ 77/77 **新建** | ✅ | 5 起始；7 风格（荒野/兽灵/复苏/月影/日怒/星辰/精火）；JSON 补 1 条；29 组重名 |
| 猎人 | ✅ | ✅（0 处 `费用`） | ✅ 77 技能 | ✅ 77/77 **新建** | ✅ | 4 起始；5 风格（射击/兽群/机敏/生存/猎鹰）；31 组重名 |
| 武僧 | ✅ | ✅（0 处 `费用`） | ✅ 130 技能 | ✅ 130/130 **新建** | ✅ | 4 起始；7 风格（极斗/踏风/织雾/无尘/锋岚/酒仙/凰火）；JSON 补 17 条；docx 删误名「金钢震」；41 组重名 |
| 游荡者 | ✅ | ✅（0 处 `费用`） | ✅ 82 技能 | ✅ 82/82 **新建** | ✅ | 4 起始；5 风格；JSON 原缺 25 条四阶等已补全；17 组重名 |
| 蛮斗士 | ✅ | ✅（0 处 `费用`） | ✅ 68 技能 | ✅ 68/68 **新建** | ✅ | 4 起始；4 风格（斗争/狂暴/生机/法咒）；26 组重名 |
| 吟游诗人 | ✅ | ✅（0 处 `费用`） | ✅ 124 技能 | ✅ 124/124 **新建** | ✅ | 5 起始技能；`静寂术` 跨风格重名；`b-skill-34` docx 无独立条目已手工对齐 |
| 魔契师 | ✅ | ✅（0 处 `费用`） | ✅ 116 技能 | ✅ 116/116 **新建** | ✅ | 4 起始；4 风格（魔契/邪念/咒能/秘术）；20 组重名；ID 前缀 `p-` |
| 奇械师 | — | ⏸ | ⏸ | — | — | **暂缓**：无人游玩，docx 未纳入 26.06.30 规则；仓库无 `基础职业-奇械师.docx`；保留现有 HTML/JSON，待有 docx 再 sync |

### 3.2 通用模块

| 模块 | docx | HTML | JSON | skill_effects | 备注 |
|------|:----:|:----:|:----:|:-------------:|------|
| 通用天赋树 | ✅ | ✅（0 处 `费用`） | ✅ 390 天赋 | ✅ 390/390 **新建** | 一~七阶；docx 删 14 条；17 个抉择标题无 `标识` |
| 特殊专长 | ✅ | ✅（无 `费用`/`标识`） | ✅ 100 条 | ✅ 100/100 **新建** | 专用 `feat_sync_core`；docx 删 4/增 5；`feat-N` 按 docx 重编 |

### 3.3 进阶途径（15 页 + Advisor L3）

| 项目 | 状态 | 数量 / 说明 |
|------|:----:|-------------|
| docx 源 | ✅ | `《基础职业进阶途径》.docx` |
| HTML | ✅ | 15 × `{职业}·进阶.html`（字段 **标识**，三色组展开） |
| 数据 JSON | ✅ | 15 × `数据/{职业}·进阶.json` |
| `advancement_data.json` | ✅ | 489 条（按职业分列，不含各页末尾重复的 10 条通用） |
| `advisor/advancements.json` | ✅ | **281** 条（按名去重合并 sourceClasses） |
| `advisor/advancement_skills.json` | ✅ | **27** documented（`advancement_details.js` 有详情的天赋树） |
| 一键命令 | — | `python scripts/apply_advancement_sync.py` |

### 3.4 汇总

- **已完成**：13 / 14 基础职业（奇械师暂缓）+ **2 / 2** 通用模块
- **有现成 skill_effects 文件**：13 个基础职业 + 通用天赋树 + 特殊专长（15 个）
- **暂缓**：奇械师（缺 docx）

### 3.5 奇械师暂缓说明

- **原因**：当前跑团中无人游玩奇械师，策划侧 **未将 `基础职业-奇械师.docx` 更新至 26.06.30 规则**（`费用→标识` 等）。
- **仓库现状**：有 `职业页/奇械师.html` 与 `职业页/数据/奇械师.json`，**无** 根目录 docx 源文件。
- **处理方式**：不强行 sync；待 docx 到位后再走标准流程（precheck → sync → verify）。
- **HTML 现状**：仍含旧 `费用：` 标签（约 95 处），与已完成的 13 职业不一致，属预期。

---

## 四、标准更新流程

按顺序执行，**每完成一个大阶段输出凭证**（见 §六）。

### Step 0：准备

1. 确认 docx 源文件在仓库根目录且为最新版。
2. 打开本文档 §三，确认该职业当前状态。
3. 阅读 `.opencode/skills/sync-class-from-docx/SKILL.md` 中 Phase 1 锚点要求（**禁止**未核实 docx 原文就跑全自动脚本）。

### Step 1：docx 原文核实（Phase 1）

对 **3–5 个代表性技能**（初始技能、有标识天赋、有等级升级、抉择子技能各一）dump docx 段落：

```bash
python -c "
import zipfile; from xml.etree import ElementTree as ET
ns='http://schemas.openxmlformats.org/wordprocessingml/2006/main'
docx=r'基础职业-{职业名}.docx'
with zipfile.ZipFile(docx) as z: tree=ET.fromstring(z.read('word/document.xml'))
paras=[]
for p in tree.iter(f'{{{ns}}}p'):
    t=''.join(x.text or '' for x in p.iter(f'{{{ns}}}t')).strip()
    if t: paras.append(t)
for i,p in enumerate(paras):
    if p=='TARGET_SKILL_NAME':
        for j in range(i, min(i+20, len(paras))): print(f'L{j}: {paras[j][:120]}')
"
```

**判定**：正确条目须在技能名后数行内出现 `施展时间：` / `关键词：` / `标识：` / `前置条件：`。  
若只有技能名连续出现 → 那是天赋树**目录行**，须向后找真正的条目。

**凭证**：`[PHASE 1 EXTRACTED: N skills]`

### Step 2：同步 HTML + 数据 JSON（Phase 2 / 2B）

1. 运行或编写 `scripts/apply_{职业}_sync.py`（战士参考 `apply_warrior_sync.py`）。
2. 脚本须完成：
   - 从 docx 提取字段、描述、等级升级、色彩标识（● 颜色）
   - **`费用：` → `标识：`**（全局替换 + 逐条 detail 重建）
   - 更新 `职业页/数据/{职业}.json` 的 `fields`、`cost`、`description`、`level_upgrades`
   -  patch 每个 `<article id="...">` 的 `.detail` 与 `data-search`
   - 复制到 `electron-app/` 镜像
3. 检查 `scripts/_warrior_sync_report.json` 同类报告：`missing_in_docx` 应为空或已解释。

**凭证**：`[PHASE 2 COMPARED: N fields]`

### Step 3：浏览器验证（Phase 3）

1. 本地打开 `职业页/{职业名}.html`。
2. 搜索/filter 抽测：初始技能（无标识）、普通战技、天赋、抉择子技能。
3. 确认：**零** `费用：`，标识圆点颜色与 docx 一致。

**凭证**：`[PHASE 3 VERIFIED: 职业页/{职业名}.html]`

### Step 4：用户确认歧义（Phase 4，按需）

以下情况**暂停并询问**，勿猜测：

- docx 与旧 HTML Mechanic 差异大且规则不明
- 技能在 JSON 中但 docx 找不到
- 抉择组子技能是否应有独立 article
- 无 `标识` 行是否为 docx 故意省略

### Step 5：同步 skill_effects JSON（Phase 5）

#### 5a. 已有 skill_effects 文件

1. 对照 `斯诺德跑团/skill_effects_schema.md` 更新每条：
   - `cost.sp` ← 标识颜色数组（如 `["橙","青"]`）
   - `cost.fp`、`cast`、`prerequisite`、`extra_condition`、`upgrades`、`tags`
2. 运行 ID 对齐脚本（战士参考 `fill_warrior_fx_gaps.py`）。
3. 确认 **JSON 中每个 skill id 在 skill_effects 中均有条目**。

#### 5b. 无 skill_effects 文件 → 见 [§七](#七首次创建-skill_effects-文件无现成文件时)

**凭证**：`[PHASE 5 JSON WRITTEN: N entries]`

### Step 6：运行验证脚本

```bash
python scripts/verify_{职业}_sync.py   # 或泛化版
```

必过项：

| 检查 | 期望 |
|------|------|
| `JSON not in HTML` | `[]` |
| `JSON not in FX` | `[]` |
| HTML 中 `费用：` | `0` |
| `fields` 含旧键 `费用` | 无 |

### Step 7：schema 对照（Phase 6）

- 新增或大幅改动的 effects 条目，核对 `skill_effects_schema.md` checklist。
- 抉择组技能加 `choices_from` 字段。

### Step 8：electron-app 镜像

确认以下文件已与主目录一致：

- `electron-app/职业页/{职业名}.html`
- `electron-app/职业页/数据/{职业名}.json`
- `electron-app/斯诺德跑团/skill_effects_{职业名}.json`（若存在）

### Step 9：更新本手册进度

**每完成一个职业或模块，必须更新本文档：**

1. §三进度表中对应行：docx / HTML / JSON / skill_effects / 验证 列改为 ✅ 或补充备注。
2. 更新文首 **「最后更新」** 日期。
3. §3.3 汇总数字。
4. 若发现新踩坑，写入 §五。
5. 若某职业**首次创建了 skill_effects 文件**，在 §3.1 备注列把 `— 无 fx 文件` 改为 `有文件 ✅`，并更新 §3.3 计数。

---

## 五、注意事项与已知踩坑

### 5.1 docx 解析

| 问题 | 处理 |
|------|------|
| 技能名出现两次（目录 vs 正文） | 正文条目近行须有 `施展时间：` 等字段；目录行跳过 |
| 同名覆盖 | 索引时取得分最高块（有 `施展时间` > 字段多 > 有标识） |
| `限制：-` 缺前缀 | 映射为 `施展限制：-` |
| 前置条件含「…战斗风格」 | 勿判为 section break（`endswith("风格")` 需排除含 `：` 的字段行） |
| 天赋无 `施展时间` | 用 `关键词：` + `标识：`  within 8 行作为入口判定 |
| 抉择子技能（如龙卷风射击） | 独立 section，父技能下用 `<p class="choice-note">` |
| docx 版本删减技能（旧 HTML/JSON 仍有） | **以 docx 为准**：`sync_class()` 自动从 HTML/JSON/FX 移除并写入 `removed_not_in_docx` |
| 萨满祭司 HTML 严重损坏（嵌套 article、`</html>` 后垃圾） | 使用 `rebuild_shaman_from_docx.py` 从 docx 重建整页；JSON 补 14 条 + 删 docx 已移除 4 条 |

### 5.2 HTML

| 问题 | 处理 |
|------|------|
| `data-search` 内嵌 HTML 引号 | 用 id 定位 article，重建 opening tag，勿用 `[^"]*` 正则 |
| 损坏的 level 升级混进 data-search | 同步时 `sanitize_data_search`  strip HTML |

### 5.3 skill_effects ID 漂移

HTML/JSON 的 article id **可能跳号**（如 `w-skill-4-4-4`、`w-skill-4-4-6`），旧 skill_effects 可能仍用连续编号（`4-4-3`、`4-4-4`）。

**处理**：

1. 以 **HTML/JSON 的 id 为权威**。
2. 按技能名匹配旧 fx 条目，执行 id 重映射（战士案例见 `fill_warrior_fx_gaps.py` 中 `ID_RENAMES`）。
3. 删除/不再保留 orphan 旧 id 条目。
4. 验证 `JSON not in FX: []`。

### 5.4 标识与 cost

- 初始技能 docx 通常**无** `标识` 行 → HTML 不显示标识行，`cost: []`。
- 部分战技 docx 也无标识（如三连斩、多重射击）→ 正常，勿强行添加。
- `cost.sp` 用短色名：`红/橙/黄/绿/青/蓝/浅/紫/粉/棕/白/黑/无`。

### 5.5 脚本不可完全无人值守

SKILL.md 要求 Phase 1 肉眼核实；全自动脚本用于批量写入，但抽取样本必须人工对照 docx。

---

## 六、阶段完成凭证

每轮同步结束，在 commit message 或 PR 描述中列出：

```
[PHASE 1 EXTRACTED: N skills]
[PHASE 2 COMPARED: N fields]
[PHASE 3 VERIFIED: 职业页/{职业名}.html]
[PHASE 5 JSON WRITTEN: N entries]
[PROGRESS MD UPDATED: 职业页同步手册.md §三]
```

---

## 七、首次创建 skill_effects 文件（无现成文件时）

**适用**：牧师、德鲁伊、猎人、武僧、游荡者、蛮斗士、吟游诗人、魔契师、通用天赋树、特殊专长等。

### 7.1 何时创建

在完成 Step 2（HTML + 数据 JSON 同步）并通过 Step 3 验证后，进入 Phase 5 时创建。

### 7.2 文件模板

路径：`斯诺德跑团/skill_effects_{名称}.json`

```json
{
  "{职业或模块名}": []
}
```

顶层键 = 职业/模块中文名（与 JSON 数据中 `"name"` 一致，如 `"战士"`、`"通用天赋树"`）。

### 7.3 逐条生成条目

对 `职业页/数据/{名称}.json` 中 **每个 skill**：

1. 读 `fields`、`cost`、`tags`、`description`、`level_upgrades`。
2. 按 `skill_effects_schema.md` 填写必填字段：`id`、`name`、`class`、`style`、`tier`、`type`、`tags`、`cost`、`effects`。
3. `type` 取自关键词首段（`战技/法术/天赋/…`）。
4. `style`：**数据 JSON / `panel_data` 用短名**（如 `塑能`、`斗争`）；**`skill_effects` 用显示名**加 `风格` 后缀（如 `塑能风格`）。`build_panel_skill_data.py` 会自动去掉多余后缀。
5. `tier` 格式 `{N}阶天赋树`。
6. `effects`：优先写精炼自然语言；可从 description 拆分，勿照抄整段 HTML。
7. 抉择组：加 `choices_from`（参见战士 `机敏的韵律` 三组抉择）。

### 7.4 批量辅助

可复用 `fill_warrior_fx_gaps.py` 中 `json_to_fx()` 逻辑，改为读取任意职业 JSON、输出新 fx 文件。  
**首次创建**时无旧 effects 可保留，effects 需从 description **新写**或半自动生成后再人工润色。

### 7.5 注册与镜像

1. 确认 `panel_engine.js` / 加载逻辑是否按文件名 `{职业名}` 加载（若硬编码列表，需追加）。
2. 复制到 `electron-app/斯诺德跑团/skill_effects_{名称}.json`。
3. 运行验证：`JSON not in FX: []`。
4. **更新本文档 §三**：该职业 skill_effects 列改为 `有文件 ✅`，§3.3 更新计数。

### 7.6 遇到全新模块（无 HTML / 无 JSON）

若 docx 对应页面尚未建立：

1. 先按 SKILL.md Phase 1B 从 docx 创建 HTML article + 导航 + JSON 条目。
2. 再创建 skill_effects 文件。
3. 在 §三新增一行，备注「从零创建」。

---

## 八、战士试点摘要（参考样本）

| 指标 | 结果 |
|------|------|
| 技能总数 | 109 |
| HTML 残留 `费用：` | 0 |
| JSON ↔ FX id 对齐 | 109/109 |
| 无标识（预期） | 6（4 初始 + 三连斩 + 多重射击） |
| ID 重映射 | 7 条（如 `4-4-3→4-4-4`，`6-6-5→6-6-6`） |
| 特殊修复 | 龙卷风射击 docx 解析、挑战怒吼 data-search |

---

## 九、推荐更新顺序

1. ~~战士~~ ✅  
2. ~~法师~~ ✅  
3. ~~术士~~ ✅  
4. ~~圣骑士~~ ✅  
5. ~~萨满祭司~~ ✅  
6. ~~猎人~~ ✅  
7. ~~牧师~~ ✅  
8. ~~德鲁伊~~ ✅  
9. ~~武僧~~ ✅  
10. ~~蛮斗士~~ ✅  
11. ~~魔契师~~ ✅  
12. ~~通用天赋树~~ ✅  
13. ~~特殊专长~~ ✅  
14. 奇械师 ⏸（暂缓，见 [§3.4](#34-奇械师暂缓说明)）  

---

## 十、变更日志

| 日期 | 内容 |
|------|------|
| 2026-07-05 | 特殊专长完成：100 条；专用 `feat_sync_core.py`；新建 `skill_effects_特殊专长.json`；docx 删 质朴/斯诺德大师等 4 条、增 5 条；`feat-N` 按 docx 重编 |
| 2026-07-05 | 通用天赋树完成：390 天赋；新建 `skill_effects_通用天赋树.json`；docx 删 14 条（抉择 E/S、番茄大厨等 12 条梗天赋）；手工清 orphan `g-skill-111` |
| 2026-07-05 | 奇械师标记 **暂缓**（无人游玩、docx 未更新） |
| 2026-07-05 | 魔契师完成：116 技能；新建 `skill_effects_魔契师.json`；20 组重名 |
| 2026-07-05 | 蛮斗士完成：68 技能；新建 `skill_effects_蛮斗士.json`；26 组重名 |
| 2026-07-05 | 武僧完成：130 技能；JSON 补 17 条四~七阶；新建 `skill_effects_武僧.json`；docx 删误名「金钢震」；41 组重名 |
| 2026-07-05 | 德鲁伊完成：77 技能；JSON 补 `d-skill-5` 荆棘术；新建 `skill_effects_德鲁伊.json`；29 组重名 |
| 2026-07-05 | 牧师完成：54 技能；新建 `skill_effects_牧师.json`；15 组重名 |
| 2026-07-05 | 猎人完成：77 技能；新建 `skill_effects_猎人.json`；31 组重名 |
| 2026-07-05 | 萨满祭司完成：98 技能；自 docx **重建** HTML/JSON/FX（原页损坏）；删 docx 移除的 4 技能 |
| 2026-07-05 | 圣骑士完成：58 技能；JSON/HTML 已对齐无需补全；21 组重名 |
| 2026-07-05 | 术士完成：48 技能；JSON 补全 4 条四阶（秘法眼/召唤次级恶魔/召唤宝石兽/慷慨术）；8 组重名 |
| 2026-07-05 | 游荡者完成：82 技能；新建 `skill_effects_游荡者.json`；JSON 补全 25 条四阶技能 |
| 2026-07-05 | 法师完成：356 技能；docx 删减技能（如 `虹光射线`）同步时自动从 HTML/JSON/FX 移除 |
| 2026-07-05 | 吟游诗人完成：124 技能；新建 `skill_effects_吟游诗人.json`；通用库 `class_sync_core.py` |
| 2026-07-05 | 创建手册；战士试点完成（HTML/JSON/FX 109/109）；记录 ID 漂移与无 fx 文件流程 |
