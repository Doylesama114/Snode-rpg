# 内容更新计划 · 战舞者 + 进阶途径刷新（完善版 v3 · 对齐新版 docx）

> **决策**：战舞者本次**直接上线**（首页 + 可建卡）；**进阶途径一并同步**；风格呈现**对齐法师的"学派分组"**。
> **本文只做计划，未改动站点代码**（只跑过只读提取，产物在 `scripts/extracts/`）。
> **v3 关键变更**：战舞者 docx 于 09-20 **22:47 再次更新**，内容与旧版差异很大 → 计划已按新版重写；v1 提取产物留档在 `_scratch/zhanwuzhe_extract_v1.json`。

---

## 一、docx 版本对比（旧 22:34 → 新 22:47）

| 项 | 旧版 | 新版 | 说明 |
| --- | --- | --- | --- |
| 文件 | 102 KB / 937 段 | **103 KB / 862 段** | 段落少 75，结构被重整 |
| 技能总数 | 53 | **54** | +1 |
| 具名数 | 52 | **58** | 缺口从 +1 扩大到 **+4**（见 §3） |
| 乱名条目 | **5 个**（"刃"×4、"共"） | **0** | 旧版解析异常的脏数据已被官方修正 |
| 同名技能内容 | — | **41 个技能内容有改动** | 例：弹射之刃、投刃、双刃回旋 → **不能基于旧版增量改**，须以新版全量落库 |
| 风格/阶分布 | — | 起始 4 + 5 风格 ×（一/二/三阶 3~4） | 刃舞 3/3/4、迷情 4/4/3、谐合 3/3/4、机敏 3/3/4、激昂 3/3/3 |
| 职业基础字段 | — | **未变** | HP 8+体质（+2+体质/级）；FP 8+关键属性（敏捷或魅力，+1/级）；护甲轻甲；武器 匕首/环刃/刺剑/拳刃/简易；豁免 敏捷·魅力；技艺 8 选 4 |
| 新增区块 | — | **起始装备 4 组（A/B/C/D）**、**战舞者专精 / 初始专长** | 创建页已有"起始装备套装"步骤（4 组）可直接对接 |

**版本冻结（执行前复核用）**
```
基础职业-战舞者.docx        SHA256 966EF09392F2F1BE10DCFF5436A65B03934DF3E730006A7741631F002CBD20A9
《基础职业进阶途径》.docx    SHA256 17C67CF3047704009484414F35B9B6D525CE147ABD4FCA880F2EB826B668AE0E
```
> docx 在 13 分钟内连改两版 → **建议冻结后再开工**；执行期间若 hash 变化，必须先重跑提取并复核差异。

---

## 二、关键事实（沿用 v2 已实测项）

| # | 事实 | 影响 |
| --- | --- | --- |
| 1 | 战舞者**从未上线**（`diff` 报 missing site data；仅存在于预告数据） | 纯新增，无历史包袱 |
| 2 | 预告数据里已有**权威字段**（key_attr"敏捷或魅力"、armor 轻甲、saves、skills 8 项） | 注册表可直接取用 |
| 3 | 进阶 docx **含「战舞者进阶途径」独立章节**（#16726） | 需**新建**战舞者进阶页 |
| 4 | 进阶 docx **另有 44 处**把战舞者列为**来源职业** | 进阶页 18 → 19 且多处更新 |
| 5 | 另一条进阶管线不受影响（`已公布进阶职业.docx` diff = 0） | 无需处理 |
| 6 | 先例：召唤师走 **M0–M7**、约 20 文件（`rebuild_summoner_from_docx.py` 675 行 + chargen/panel/class_entries 三套脚本） | 战舞者按同节奏执行 |

### 数据契约

| 位置 | 形状 | 战舞者要做的 |
| --- | --- | --- |
| `职业页/数据/classes.json` | **数字键** 0..16（17 项）：`{name, description, 职责定位, 关键属性, 护甲, 武器, 豁免, 技巧}` | **追加键 17**（不动既有） |
| `职业页/数据/classes_data.js` | 由 classes.json 生成 | 重建 |
| `职业页/数据/class_features.json` | 职业特性/抉择组 | 新增条目 |
| `职业页/数据/future_classes_data.json` | `{meta, classes:[{id,status,playable,…}]}` | 战舞者 → `published / playable:true` |
| `斯诺德跑团/panel_data.js` | `REF_CLASSES = JSON.parse('{…}')`（**JSON 字符串**）+ `SKILL_DATA` | 加条目 + 54 条技能（含 `specializations`/`starting_features`） |
| `斯诺德跑团/chargen_adv_paths.js` | 期望进阶路线候选（14 职业 585 条） | 加战舞者 |
| `职业页/advancement_details.js` | 进阶总表 216 KB | 由进阶同步重建 |
| 创建页装备步骤 | 已有"起始装备套装（4 组）"（`角色创建页.html` L2489/2520） | 补战舞者 **A/B/C/D** 四组 |

---

## 三、M0 必须先澄清的数据质量项

| 项 | 现象 | 处理 |
| --- | --- | --- |
| **具名 58 vs 实际 54** | 提取器发现 58 个"名字"，只生成 54 个技能块 → **4 个名字无对应技能卡** | M0 用 `extract_class_docx.py` 的按风格列表输出逐个核对：是"交叉引用/表格表头"（正常）还是"漏解析"（需修提取器或转 issue） |
| 旧版乱名已消失 | 旧版 5 条脏数据（"刃"×4、"共"）新版无 | 确认无需脏数据迁移 ✓ |
| 41 技能内容变更 | 新版大幅修订 | **全量重落**，禁止增量 patch |
| 起始装备/专精新块 | 新内容 | M1 落 4 组装备；M5 落专精与初始专长字段 |

---

## 四、执行里程碑（M0 → M9）

### M0 · 冻结与基线
- 记录并**复核 docx SHA256**（见 §1）；确认冻结
- 备份：`备份区/2026-09/内容更新_战舞者_v1.0.8003.zip`（`职业页/`、`斯诺德跑团/`、`electron-app/`）
- gap report：docx ↔ 站点缺失项；列出 **4 个"具名无卡"** 并定性
- **判据**：缺口清单全部有结论；`git status` 基线 = tag `v1.0.8003`

### M1 · 生成器 + 数据 + 页面（按新版全量）
- 新写 `scripts/rebuild_zhanwuzhe_from_docx.py`（照 `rebuild_summoner_from_docx.py`）：
  - `职业页/数据/战舞者.json`：**54 技能**（字段 name/style/tier/type/fields/cost/description/level_upgrades/flavor/field_runs/description_entries/source）
  - `职业页/战舞者.html`：**模板取 法师.html**（风格分组）；5 风格 × 三阶 + 起始
  - **起始装备 4 组（A/B/C/D）** 结构化输出（供创建页使用）
  - 同步 `electron-app/职业页/`
- **判据**：`diff_class_extract.py --class 战舞者` = 0；页面 54 卡、5 风格切换正常；装备 4 组数据完整

### M2 · 注册与榜单
- `classes.json` 追加键 `17`；重建 `classes_data.js`、`class_features.json`；检查 `equipment_data.js` 是否含**环刃/拳刃/刺剑**
- `future_classes_data.json` 转正；`首页.html` 基础职业 **14 → 15**
- **判据**：首页卡片可进；预告区不再显示战舞者；`verify_class_lazy_e2e.mjs` 通过

### M3 · 搜索索引
- `node scripts/build_class_search_index.js`；**判据**：搜索命中 54 条

### M4 · 创建页接入
- 新写 `scripts/apply_zhanwuzhe_chargen_entries.py`：职业选择、**关键属性二选一（敏捷/魅力）**、技艺 8 选 4、**起始装备 A/B/C/D**、期望进阶路线候选
- 改 `角色创建页.html`、`上传角色.html`
- **判据**：新增 `verify_zhanwuzhe_chargen_e2e.mjs` 全绿；完整建卡走通到上传

### M5 · 角色面板接入
- 新写 `scripts/apply_zhanwuzhe_panel_entries.py`：`panel_data.js`（REF_CLASSES + SKILL_DATA 54 条 + `specializations`/`starting_features`）、`panel_engine.js`（5 风格分栏）
- 重建 `build_panel_skill_data.py` 产物
- **判据**：面板风格分栏/筛选/学习/存档正常；`verify_panel_data_sync.py`、`verify_class_weapon_profs.py` 通过

### M6 · 进阶途径同步
- `python scripts/apply_advancement_sync.py`：
  1. **新建** `战舞者·进阶.html/json`（docx #16726）
  2. 更新现有 18 页中 **44 处含战舞者的来源职业**
  3. 重建 `advancement_details.js` + 顾问索引；复核 `scripts/_advancement_sync_report.json`
- **判据**：`verify_advancement_branches.py`、`verify_published_advancement.py` 通过；进阶页 **18 → 19**

### M7 · 顾问与检索
- 重建 advisor 索引；**判据**：顾问可检索战舞者技能与进阶路径

### M8 · 镜像 + 全量门禁
- SYNC 到 `electron-app/`；核对 `search-index.json` 双端一致
- `node verify_all.mjs` → **Clean · Errors 0**；新增 e2e 并入 `verify_all`

### M9 · 发版前核验与发布
- **截图核验**：首页 15 职业 / 战舞者页（5 风格）/ 战舞者进阶页 / 建卡（含装备 4 组）/ 面板风格分栏 / 搜索命中
- `bump-version.js 1.0.8004` → 提交 → tag `v1.0.8004` → 推送 → CI 三端 → 产物/镜像核验 → 文档补发布记录

---

## 五、顺序约束

```
M0 → M1 → M2 → M3 → M4 → M5 → M6 → M7 → M8 → M9
                              ▲
   M6（进阶）必须在 M1/M2 之后：进阶的 44 处「来源职业 … 战舞者」
   要求该职业已存在于站点、注册表与搜索中
```

---

## 六、风险与对策（v3 更新）

| 风险 | 说明 | 对策 |
| --- | --- | --- |
| **docx 仍在迭代** | 13 分钟两版；旧版 41/53 技能随后被改 | 冻结 hash 后开工；执行中若变化 → 重跑提取 + 重做差异核对 |
| 4 个"具名无卡" | 可能是交叉引用，也可能漏解析 | M0 定性；若是漏解析则先修提取器再落数据 |
| 硬门禁不支持新职业 | `apply_class_extract.py` 仅支持特定 class+tier | 照召唤师先例写专用生成器，先在 `_scratch/` 试跑 |
| 双关键属性（敏捷**或**魅力） | 创建页加值/校验可能只支持单属性 | 先查先例；无则扩展并**回归 14 个既有职业** |
| `classes.json` 数字键顺序 | 插错位置打乱榜单 | 只追加键 17 + 断言"17 项且索引连续" |
| 41 技能内容修订 | 增量改会残留旧文本 | **全量重落**；`verify_skill_text_format.py` 校验每条描述 |
| 起始装备 4 组 | 需对接已有装备步骤的数据结构 | M1 先读创建页装备步骤的数据来源，按其 schema 产出 |
| `panel_data.js` 是 JSON 字符串 | 手改易破转义 | 只由脚本注入；改后跑 `verify_panel_data_sync.py` |
| 搜索/首屏性能 | +54 卡片 | M3 后立刻跑懒渲染 e2e（含性能断言） |
| 进阶改动面大 | 18→19 页、44 处引用 | sync report + git diff 复核；抽样核对来源职业 |

---

## 七、回滚

- 每里程碑一次提交（`feat(zhanwuzhe): M1 …`）→ 可单点 revert
- M0 备份 zip 覆盖三大目录；**门禁不过不发 tag**，线上保持 v1.0.8003

---

## 八、待办清单

- [ ] M0 hash 冻结复核 + 备份 + gap report（含 4 个"具名无卡"定性）
- [ ] M1 `rebuild_zhanwuzhe_from_docx.py` → 54 技能 JSON + 页面 + 装备 4 组
- [ ] M2 classes.json(17) / classes_data.js / class_features.json / 预告转正 / 首页 15 / 装备表检查
- [ ] M3 search-index 重建（+54）
- [ ] M4 创建页（二选一属性 / 8 选 4 技艺 / 装备 A–D）+ `verify_zhanwuzhe_chargen_e2e.mjs`
- [ ] M5 面板 REF_CLASSES + SKILL_DATA(54) + 专精/初始专长 + 风格分栏
- [ ] M6 进阶同步（新建战舞者进阶页 + 44 处来源职业 + 总表）→ 19 页
- [ ] M7 顾问索引
- [ ] M8 镜像 + `verify_all` 全绿 + 新 e2e 并入门禁
- [ ] M9 截图核验 → bump 1.0.8004 → tag → CI → 产物/镜像核验 → 发布记录
