# 斯诺德全职业技能树 - 后续更新教程

> 当前版本：v1.047  
> 更新日期：2026-05-31（v1.046）

---

## 1. 项目结构概览

```
scholar-agent-main/
├── 斯诺德跑团/                     # 角色系统（主要工作区）
│   ├── 启动台.html                 # 统一入口
│   ├── 角色面板.html               # 核心角色卡（含 SKILL_DATA、REF 数据）
│   ├── 角色创建页.html              # 创建角色向导
│   ├── 角色选择页.html              # 角色存档选择
│   ├── 角色存档页.html              # 存档管理
│   ├── 上传角色.html               # xlsx 角色卡上传/导入
│   ├── 主页.html                   # 主页
│   ├── 帮助.html                   # 规则帮助文档
│   ├── 警告.jpeg                   # 上传警告图片
│   └── 导出立绘.js                  # OOXML 立绘导出模块
├── 职业页/                        # 职业技能浏览
│   ├── 首页.html                  # 主页面
│   ├── {职业名}.html               # 各职业技能页（15 职业）
│   ├── {职业名}·进阶.html           # 各职业进阶页
│   ├── 通用天赋树.html              # 通用天赋树
│   ├── 数据/                       # JSON 数据文件
│   │   ├── classes.json           # 职业定义
│   │   ├── races.json             # 种族数据
│   │   ├── backgrounds.json       # 背景数据
│   │   └── {职业名}.json           # 各职业技能详情
│   ├── common.css                 # 公共样式
│   ├── common.js                  # 公共脚本
│   └── filter.js                  # 筛选功能
├── electron-app/                  # 桌面版打包项目
│   ├── 斯诺德跑团/                  # ⚠️ 打包时自动同步，不要手动修改
│   ├── 职业页/                     # ⚠️ 打包时自动同步，不要手动修改
│   ├── main.js                    # Electron 主进程（入口：斯诺德跑团/启动台.html）
│   ├── electron-builder.yml       # 打包配置
│   └── package.json
├── build_skill_lookup.js          # SKILL_LOOKUP / SKILL_TIER / SKILL_STYLE 重建工具
├── 发布版文档/                     # 正式发布文件
├── 备份区/                        # 旧文件备份
├── 项目文档.md                     # 项目文档 & 开发历史
├── 后续更新教程.md                  # 本文件
├── 关键词系统注意事项.md             # 关键词筛选规范
└── 工作规则.md                     # 编码规范
```

---

## 2. 修改技能页面（职业页/）

### 2.1 直接编辑 HTML

所有技能页面在 `职业页/` 目录下，直接用文本编辑器修改：

- **技能页面**：`职业页/圣骑士.html`、`职业页/德鲁伊.html` 等
- **进阶页面**：`职业页/圣骑士·进阶.html`、`职业页/术士·进阶.html` 等

### 2.2 修改后必须同步

**❗ 重要：打包 exe 前必须将改动同步到 `electron-app\职业页\` 目录下**，否则打包后的 exe 不会包含你的修改。

---

## 3. 修改角色系统（斯诺德跑团/）

### 3.1 核心文件

| 文件 | 说明 |
|------|------|
| `角色面板.html` | 核心角色卡（~2.3MB），包含全部前端逻辑、SKILL_DATA、REF_CLASSES/RACES/BACKGROUNDS 等 |
| `角色创建页.html` | 创建角色向导（种族选择、职业选择、语言系统、装备系统等） |
| `上传角色.html` | xlsx 冒险者角色档案上传/导入，含自动纠错、阶位违规检测、数据校验 |
| `角色选择页.html` | 角色存档列表，切换/删除/清空角色 |
| `启动台.html` | 统一入口（角色系统 + 职业技能 + 帮助文档） |

### 3.2 修改技能数据后必须重建查找表

`build_skill_lookup.js` 是独立 Node 脚本，从 `角色面板.html` 的 SKILL_DATA 和 `职业页/数据/*.json` 中提取所有技能名并重建查找表：

- **SKILL_LOOKUP**：技能名 → [职业名, 是否为天赋] 的映射
- **SKILL_TIER**：技能名 → 阶位 的映射

**修改技能数据后必须运行** `node build_skill_lookup.js` 更新 `上传角色.html` 中的查找表。

---

## 4. 搜索高亮功能说明

进阶页面的搜索功能已改造为**匹配内容高亮闪烁**：

- 搜索时，匹配文字的可见部分会包裹 `<span class="search-highlight">`
- 样式：金色底色 `#FFD700`，红黄交替闪烁3次
- 清空搜索框时自动清除所有高亮
- 如果添加新的进阶页面，需要确保：
  1. 搜索输入框 `id` 格式为 `{职业名}-adv-search`
  2. 每个卡片 `article` 需要有 `data-search` 属性
  3. 搜索脚本使用 `oldHL / search-highlight` 机制（参考已有页面）

---

## 5. 重新打包 exe

### ⚠️ 必须走完整 electron-builder 管道

**禁止使用 `--prepackaged` 方式打包。** 手动构建 `resources/app/` 目录会导致 exe 无法运行。
必须让 electron-builder 自动创建 `app.asar`。

### 打包流程

```powershell
# 1. 清除旧的构建产物
Remove-Item -Recurse -Force electron-app\dist -ErrorAction SilentlyContinue

# 2. 同步最新的源文件到 electron-app
node -e "
var fs=require('fs');var p=require('path');
function copy(s,d){
  if(!fs.existsSync(d))fs.mkdirSync(d,{recursive:true});
  fs.readdirSync(s,{withFileTypes:true}).forEach(function(i){
    if(i.name.indexOf('backup')>=0)return;
    var sp=p.join(s,i.name),dp=p.join(d,i.name);
    i.isDirectory()?copy(sp,dp):fs.copyFileSync(sp,dp);
  });
}
copy('../斯诺德跑团','斯诺德跑团');
copy('../职业页','职业页');
"

# 3. 运行完整 electron-builder 管道
cd electron-app
npm run dist
```

### 打包前提
1. 确保 `electron-app\package.json` 开头没有 UTF-8 BOM
2. 确保已同步最新源文件到 `electron-app\斯诺德跑团\` 和 `electron-app\职业页\`

### 打包产物
便携版 exe 生成在 `electron-app\dist\斯诺德跑团.exe`。

### 发布新版本

1. 将 exe 复制到根目录并加上版本号（`斯诺德跑团_v1.046.exe`）
2. 同时生成纯 HTML zip 版（`斯诺德跑团_v1.046.zip`，排除 backup 文件）
3. 更新 `项目文档.md` 中的版本号和开发历史

---

## 6. 常见问题

### Q: 修改后 exe 里没生效？
A: 八成是只改了根目录，没同步到 `electron-app\`。打包前必须同步最新文件。

### Q: 进阶页面搜索没有高亮？
A: 检查页面中是否包含：
- CSS：`.search-highlight` 和 `@keyframes search-blink`
- JS：`oldHL = document.querySelectorAll(".search-highlight")` 逻辑
- 搜索输入框 id 格式正确

### Q: 上传角色时技能识别不到？
A: 八成是 SKILL_LOOKUP 没更新。修改技能数据后运行 `node build_skill_lookup.js`。

### Q: combined_index.html 哪去了？
A: 已归档到 `备份区\`。从 v1.03 开始，分发方式改为单文件 exe / zip，不再使用 combined_index.html。

### Q: 为什么 electron-app 里的文件不能直接改？
A: 打包前会用根目录的源文件覆盖 electron-app 副本，直接改会被覆盖掉。

---

> 如有其他问题，请查看 `项目文档.md` 和 `工作规则.md` 获取更多信息。
