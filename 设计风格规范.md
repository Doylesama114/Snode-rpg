# 斯诺德跑团 · 设计风格规范（v1 · 2026-09-20）

> 本文是**最终定稿方向**（v7 被用户与作者共同认可）的可执行规格。
> 以后任何界面改动都先读本文；**新增数值必须先在这里登记**，再写进代码。
> 参考图库：`测试型更新/参考图/`（炉石 8 / TRPG 20 / 方舟 24 / 其他），草案演进：`测试型更新/导航草案v2…v7/`

---

## 一、基调

**明亮木质 · 冒险者工会**：像走进一间采光良好的木工坊/工会前台，而不是阴暗酒馆地窖。
三个关键词：**明亮**（底色是暖木，不是黑）、**实体**（东西有厚度、能抠能按）、**克制**（装饰只出现在结构与交互上）。

## 二、四层 + 两态（立体感的来源）

| 层 | 是什么 | 做法 |
| --- | --- | --- |
| 0 场景/底 | 明亮橡木墙 | 底色 `#d0a97a` + 竖木纹 + 每 172px 横板缝 + 顶部暖光；四周轻微压角（中央透明） |
| 1 板/柜 | 木框、柜台 | 顶面 1px 白高光 + 底部 2~6px 暗边（**非对称阴影 = 厚度**）+ 大外投影 |
| 2 凹 | 嵌进板里的格位、羊皮纸 | `inset` 内阴影（`--sink`） |
| 3 凸 | 可点的木牌、按钮 | 外投影 + hover 抬起 / active 内陷（`--lift` / `--lift-hi`） |
| 4 浮 | 下拉面板、提示 | 最大投影 `0 34px 64px` + 遮罩 |

**统一顶光**：所有元素上沿有一道浅色高光、下沿有一道深色暗边。禁止"上暗下亮"。

## 三、令牌（唯一数值来源）

```css
/* 色 */
--wood-1:#6b4a28; --wood-2:#54381c; --wood-3:#3f2913;   /* 深木：顶梁/外框 */
--plank-1:#e8cfa4; --plank-2:#d9b982;                     /* 浅木：柜台面/木牌面 */
--parch-1:#fdf8ec; --parch-2:#f3e9d4;                     /* 羊皮纸 */
--cork:#c9ad7f;                                           /* 告示板底板 */
--gold:#c8a25a; --brass-hi:#e6c37c; --brass-lo:#a8802f;   /* 黄铜 */
--seal:#a83226;                                           /* 火漆红：图钉/强调，仅此一处 */
--ink:#2f2317; --ink-2:#6d5942;                           /* 文字两级 */
/* 阴影（凹凸两态） */
--sink: inset 0 2px 5px rgba(90,60,20,.32), inset 0 -1px 0 rgba(255,255,255,.5);
--lift: 0 5px 12px rgba(40,24,6,.38), inset 0 1px 0 rgba(255,255,255,.75);
--lift-hi: 0 14px 26px rgba(40,24,6,.46), inset 0 1px 0 rgba(255,255,255,.85);
/* 字号 / 间距 */
标题 42px(hero)/17px(木牌标题)/15px(纸条标题)；正文 14~15px；说明 11.5~12.5px
间距只用 4 的倍数：4 / 8 / 12 / 16 / 24 / 32 / 48
圆角：木牌 4~7px（切角形状由 SVG 决定）、纸条 2px、面板 8~9px
```

## 四、SVG 手绘规范（本项目的核心特征）

- **一律内联 `<symbol>` + `<use>`**（无外部文件，避免 file:// 与打包路径问题）；集中在页面顶部一个 `<svg width="0" height="0">` 里。
- **按钮形状**：不用矩形。木牌用切角路径（左上/右下切角）+ 顶部 1px 白斜面 + 底部暗边 + 2~3 条木纹线，`preserveAspectRatio="none"` 随容器拉伸。
- **图标**：24×24 描边式（`stroke-width:1.8`，圆头圆角），`color` 用 `currentColor` 继承。现有 12 枚：人像/书/骰子/问号/加号星/上传/存档柜/书架/药瓶/天赋树/靶心/罗盘。
- **小部件**：图钉（红帽+高光+钉脚）、四角钉（三层圆）、纸角折痕、挂绳（贝塞尔曲线）、公会徽记。
- **新增图形**必须：① 与既有线宽一致 ② 只用一个色 `currentColor` ③ 命名 `#i-xxx` / `#xxx`。

## 五、组件规格

| 组件 | 关键尺寸 |
| --- | --- |
| 木牌按钮 | 高 ≥84px；图标 44px 居中左；文字左内边距 66px；箭头 26px 右侧 |
| 凹槽格位 | padding 11×13px；内阴影 `--sink`；hover 微浮 1px |
| 纸条 | padding 14~16px；旋转 ±1.2°（`3n` 再错位 4px）；图钉在顶部居中 -11px |
| 告示板 | 外框 padding 16px；底板 padding 20~22px；四角钉 14px |
| 下拉面板 | 宽 ≤920px；高 ≤78vh；`chips` 一行 5 个；设置行贴底 |

## 六、反模式（走过的弯路，别再犯）

1. **❌ 用照片当卡片画面**——自有插图明度/构图不统一，塞进卡片必打架（v3 教训）。
2. **❌ 冷色极简**——把参考站的个人作品风格套到游戏工具上，丢了温度（v4 教训）。
3. **❌ 纯方框按钮 + 只有一层关系**——"平铺"的根因（v2 教训）。
4. **❌ 整体压暗场景当底**——用户反馈"太暗"（v5 教训）。
5. **❌ 没有设计系统就动手**——每版重新发明间距字号，导致反复返工。
6. **❌ 一次回复塞整份大文件**——会触发输出上限被截断，要拆多次写入。

## 七、素材许可

| 素材 | 许可 | 要求 |
| --- | --- | --- |
| 自绘 SVG（图标/木牌/钉子等） | 项目自有 | 无 |
| game-icons.net 图标（如需扩充） | CC BY 3.0 | 署名："Icons made by Lorc and Delapouite. Available on https://game-icons.net" |
| 世界观插图（help-media） | 项目自有 | **仅可用于背景/插画，不用于卡片缩略图** |
| 音频（Kenney 等） | CC0 | 见 `docs/audio-licenses.md` |

## 八、实施纪律

- 任何页面改动前：**先并排对比**（原版 vs 新版），只有明显更好才采用。
- 每次改动只动**一个变量**（颜色 / 尺寸 / 结构），便于判断效果来源。
- 新增组件先在 `测试型更新/` 里出草案，验证后再铺到正式页面。
- 数值变更必须回写本文档（第三节）。

---

## 九、实施状态（2026-09-20）

| 项 | 状态 | 位置 |
| --- | --- | --- |
| 共享层（样式 + 图标 + 导航 + 配置） | ✅ 已落地 | `斯诺德跑团/guild-ui.css`、`斯诺德跑团/guild-ui.js` |
| 启动台（柜台 + 告示板） | ✅ 已按 v7 重做，按配置渲染 | `斯诺德跑团/启动台.html` |
| 设置页（启动台自定义 + 偏好 + 关于·许可） | ✅ 新建 | `斯诺德跑团/设置.html` |
| 页面铺开 | ✅ 50 页各加 1 行 `<script src="guild-ui.js">` | `斯诺德跑团/`、`职业页/` |
| 移动端 | ✅ 媒体查询（木牌单列 / 纸条单列 / 面板全高） | `guild-ui.css` |
| 深色模式 | ✅ `html.dark` 木色压暗 + 文字转浅 | `guild-ui.css`、`启动台.html` |
| 验收 | ✅ `verify_guild_ui_e2e.mjs` 27P/0F，并已接入 `verify_all.mjs` | 仓库根目录 |

**新增页面时的接入方式**：只需在 `</body>` 前加一行
```html
<script src="guild-ui.js"></script>          <!-- 斯诺德跑团/ 下 -->
<script src="../斯诺德跑团/guild-ui.js"></script>  <!-- 职业页/ 下 -->
```
样式、图标、导航按钮、主题应用会自动完成。

### 页面统一进度（2026-09-20 第二批）

| 页面 | 状态 | 做法 |
| --- | --- | --- |
| 启动台 | ✅ 完全重做 | 柜台 + 告示板（按配置渲染） |
| 主页（冒险者手册） | ✅ 完全重做 | 木梁 + 英雄区 + 柜台（4 张 SVG 木牌） |
| 资料库 / 角色选择页 / 角色存档页 / 角色创建页 / 上传角色 / 物资大全 / 顾问 / help | ✅ 套壳 + 适配 | 开 `window.__guiSkin` → 木质底 + 木梁；由 **旧页面适配器**（guild-ui.css 末尾）统一卡片/按钮/标题 |
| **角色面板** | ✅ 套壳 + 页头 | 木梁式页头 + 羊皮纸卡片；夜间自动切回中性深色；战斗态保留原紫黑主题 |
| **职业页（38/39 页）** | ✅ 全部套用 | 木梁顶栏 + 羊皮纸目录/卡片；首页另有木牌标题栏与分区层级。未套用 `_monk_test_top.html`（测试页，打包已排除） |

**新增旧页面时**：只要 `<body>` 上会被 guild-ui 加上 `.gui-page`（`window.__guiSkin = true`），适配器就会接管：
隐藏重复返回链接 → 卡片变羊皮纸+木描边 → 按钮变黄铜牌 → 标题与木梁重复时自动隐藏。

**改动源目录后必须同步镜像**（否则 `verify_all` 的镜像一致性校验会失败）：
```bash
node _scratch/sync_mirror.mjs      # 或重跑发布脚本的整体拷贝
```

### 职业页首页（索引页）规范

| 元素 | 规格 |
| --- | --- |
| 分区标题 | **木牌标题栏**：深木渐变 + 米色衬线标题 + 金色拉丁副标（BASE CLASSES / GENERAL / ADVANCED）+ 右延细线；圆角 6px，下边框 3px |
| 格子 | **凸起羊皮纸牌**：`linear-gradient(180deg,#fffdf6,#f4e9cf)` + 木色描边 `#c9ab74` + 顶光/底暗边；hover 抬起 3px 且描边转黄铜；active 内陷 |
| 图标 | **保留彩色 emoji**（原版写法），坐进"刻出的圆槽"（46px 圆形 + 内阴影），字号 24px |
| 层级区分 | 基础=默认；**通用**=黄铜描边+铜色圆槽；**进阶**=深木描边 + 顶部铜色带 + 右下角「进」标记 |
| 页面背景 | 交给共享层 `.gui-page`（单一来源）；**禁止**页面自己再写一份 body 背景（曾导致 `fixed` 覆盖与整页截图断裂） |
| 背景附着 | 统一 `background-attachment: scroll`（长页面连续 + 免固定背景重绘） |

### 向导页与顾问页（补充规范）

| 场景 | 规格 |
| --- | --- |
| 角色创建向导 | 步骤条：pending=半透明木、**done=黄铜渐变**、**active=深木加粗**；步骤标题衬线化；右侧「角色概览」面板羊皮纸 |
| AI 顾问 | 自带 `header.hdr` 改为木梁；聊天区 `#app` 由象牙白改羊皮纸（夜间中性深色） |
| 木梁注入边界 | `beamBoot` 在 **body 为 grid/flex 时跳过**——该布局下注入的 header 会被自动排版到其它网格单元（曾跑到页面底部） |
