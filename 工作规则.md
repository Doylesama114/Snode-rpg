# 工作规则

## 1. 发布流程
- **最新 tag**：`v1.0.7243`（2026-08-14，角色创建页 BG3 式重构 + 顾问角色分析）
- 版本号格式 `1.0.xxxx`，末尾递增不跳号（当前线上参考 `后续更新教程.md` 头部）
- 版本号统一使用 `node bump-version.js x.x.x "改动描述"` 更新
- **职业页内容改动**：先走写站（sync 或 extract→diff→apply）→ 本地验证 → 再 bump；详见 `后续更新教程.md` §1.5 与 `职业页同步手册.md` §四点五 / §四点六
- 发布前至少：`node verify_all.mjs`（44 页 + tests.html 870 断言 + 数据一致性 + **视觉检查**：截图→视觉模型，免费 mimo 优先、失败降级百炼，高严重度问题阻断；`VERIFY_VISUAL=0` 可跳过）；涉及 bard 五阶时再跑 `python scripts/verify_bard_sync.py`
- 打 tag `v*` 推送到 GitHub 自动触发 Release 构建
- GitHub Secrets（`GH_TOKEN`、`GITEE_TOKEN`）不得提交到仓库
- 所有 Token、密钥、密码通过 GitHub Secrets 注入，不在代码中硬编码
- 发版后同步：`后续更新教程.md`（根+electron-app）、`项目文档.md` §1.2/§12、本文件最新 tag

## 2. 代码规范
- Electron 中不用 `alert()` / `prompt()`——`contextIsolation: true` 下不显示
- 中文文本只放页面正文，HTTP 头只能用 ASCII
- 所有新建 HTML 页面需要在 `</body>` 前加载脚本
- Bug 反馈通过 `preload.js` IPC 桥接到主进程发网络请求
- 不要用 PowerShell 操作含有中文的文件内容——UTF-8 编码会被破坏

## 3. 打包
- 使用 `electron-builder.yml` 中的 NSIS 配置
- `oneClick: false` + `perMachine: false` = 可选安装目录
- CI 构建需要在 `release.yml` 中预装 NSIS：`choco install nsis -y --no-progress`
- electron-builder v26+ 不再捆绑 NSIS 二进制，系统必须已安装 makensis
- 打包产物：exe + latest.yml + blockmap
- 发布到 GitHub Releases 后，用户端自动检测更新
- 不要在 CI 中使用 `spawn_monkey_patch.js`——与 v26 NSIS 模板不兼容

## 4. 备份
- 备份区目录用于存放历史备份，不得覆盖更新
- 备份文件使用带时间戳的文件名

## 5. 文档
- 大改动后更新 `项目文档.md`、`后续更新教程.md`、`AGENTS.md`；职业技能管线改动同步 `职业页同步手册.md`
- 卡牌游戏 UX 改动同步更新上述 MD + 本文件 §0.5 最新 tag
- changelog 由 `bump-version.js` 自动维护，不需要手动编辑
- `electron-app/后续更新教程.md` 与根目录 `后续更新教程.md` 保持同文（发版前复制对齐）

## 8. 角色面板 · 色彩标识（26.06.30）

- **v1.0.6118** 已完成：`sp_points` + `color_marks`、wildcard 学习校验、GM 作弊面板调 SP/标识
- **不实现**：冥想/关键偏好/专长等「游玩结束授予标识」的自动效果（辅助软件，桌面团 DM 结算）
- **待办**：DM 团队奖励结算页（清单 + 批量写入），见《职业页同步手册》§一

## 6. 斯诺德对决（poker-game-main）发版
- 构建：`cd poker-game-main && pnpm run build`
- 同步：`robocopy poker-game-main\dist electron-app\poker-game /MIR`
- 版本：`node bump-version.js 1.0.60xx "条目1;条目2"`（仓库根目录）
- 卡组必须为 **恰好 15 张** 方可保存并进局（`deckValidation.ts`）
- 筛选逻辑：`deckPoolFilter.ts`（类别/属性 OR，关键词 AND，跨维度 AND）

## 7. 排错经验（2026-06-14 反思记录）

### 7.1 搜索文本模式时必须覆盖所有变体
- 游荡者页等级描述存在两种写法：
  - `你的游荡者职业等级到达X级时`（70处，天赋树技能）
  - `当你的游荡者等级达到X级时`（22处，起始特性）
- **教训**：不能只搜一种文本模式。应搜最小公共子串（如 `级时`）再反向筛选遗漏项。
- **检查方法**：从页面第一个技能开始验证，而非从中间或末尾。

### 7.2 用户反馈与自动化验证冲突时，优先怀疑自动化覆盖不全
- Playwright 报 `font-weight: 700 `，用户坚持没加粗 → 原因是 Playwright 只测了一种文本模式，漏了另一种。
- **教训**：用户说"没看到"时，先问"你看到的是哪个技能？"定位具体实例，再查那一个实例的原始 HTML。

### 7.3 Changelog 漏写防范
- 多个版本连续漏写 changelog 的根本原因：发版流程没有 changelog 检查步骤。
- **规则**：每次打 tag 前必须确认 `changelog.js` 和 `斯诺德跑团/changelog.js` 均已写入当前版本条目。

### 7.4 内容结构修复必须验证标签平衡
- 孤儿 section 归位时，用 `git checkout` 回退了一次破坏性编辑（多出 4 个 `</section>`）。
- **教训**：任何 section/div 级别的内容移动后，必须用脚本验证开闭标签数量平衡。

### 7.5 导航 link 归属检查
- 抉择技能（侦测毒性与疾病等）被错误添加到所有 5 个风格的 一阶 nav 中。
- **教训**：检查 nav link 归属时，对照 docx 地面真值确认每个技能的唯一所属风格。

### 7.6 docx 表格结构下的技能数据提取
- **问题**：武僧 docx 用嵌套表格存储技能数据（表头行列技能名 → 表体行详情），文本距离匹配（搜技能名→找最近"费用"）因表头行多个技能名相邻而串数据。
- **失败方案**：
  - 批量文本匹配：技能名集中在表头行，文本距离匹配到错误的费用行
  - python-docx 表格解析：嵌套表格+跨行列复杂度超出库能力
- **可行方案**：用技能的**唯一描述文本**（如"猛踏地面释放强而有力的冲击波"）在 docx 中定位，向前搜索最近的 `费用`+颜色标签。
- **教训**：docx 提取优先信任描述文本定位而非技能名定位；JSON 数据不可盲目信任——必须以 docx XML 颜色标签为最终地面真值。

### 7.7 Phase 1B 新建 tier 时的 edit 工具匹配陷阱
- **问题**：使用 `edit` 工具在 HTML 中插入新建 tier 内容时，`oldString` 使用了 `</div>\n</article>\n</div>\n</section>` 这种通用闭合模式。该模式在文件中**多次出现**（每个 section 结尾都有），`edit` 工具会匹配**第一处**出现，导致新 tier 被插入到错误的 section（通常是起始特性区或第一个风格区）。
- **已发生案例**：
  - 附魔六阶（m-tier-5-6）插入到起始特性 section 内部（v1.0.5140）
  - 幻术五阶（m-tier-7-5）插入到起始特性 section 内部（v1.0.5141）
- **避免方法**：
  1. **oldString 必须包含足够的上下文**，使其在文件中唯一。例如使用上一个 tier 的最后几行 + `</section>` 作为锚点
  2. 示例正确做法：`oldString` 应包含前方的 tier div id（如 `<div class="tier" id="m-tier-7-4">` 闭合标签）以确保匹配到正确的 section
  3. **优先使用脚本**而非 `edit` 工具执行搬迁——Python 的 `find()` 可以精确定位任意位置
- **检测方法**：
  ```python
  # 验证 tier 在正确 section 内
  tier_pos = html.find('id="m-tier-X-Y"')
  section_start = html.find('id="m-style-X"')
  next_section = html.find('id="m-style-Y"')
  assert section_start < tier_pos < next_section
  ```
- **修复方法**：使用 Python 脚本提取错位 tier 块 → 从错误位置切除 → 插入到正确 section 的 `</section>` 之前

### 7.8 全新创建技能后的导航验证（强制）
- **规则**：每次 Phase 1B 全新创建 tier 后，**必须**执行以下两项验证，通过后方可提交：
  1. **Nav ID 匹配**：nav 侧栏的 `href` 值与 content 区 `article id` 一一对应，无缺失无多余
  2. **Section 位置**：tier div 的 DOM 位置在其所属 style section 内部（`m-style-X` 与 `m-style-Y` 之间）
- **验证脚本参考**：`check_i5nav.py`（ID 匹配） + `check_pos.py`（位置检查）

### 7.9 Phase 1 锚点跳过 = 批量数据灾难（2026-06-29 反思）

- **问题**：萨满祭司火焰风格 T2/T3 费用颜色修正时，使用 XML body-element 运行级颜色提取脚本，输出显示 13 技能全部"红+蓝"。据此修改了 7 个原本颜色正确的技能为蓝色。
- **根因**：docx 中技能名出现两次（tier 标题列表 + 实际技能条目），XML body-element 脚本全命中 tier 标题列表所在 body，提取到的颜色不可信。SKILL.md 第 85-112 行明确警告"NEVER trust automated scripts""Always read raw docx lines directly"，我完全跳过。
- **损失**：7 技能 HTML + JSON + skill_effects 三文件均需还原，版本 v1.0.5182→v1.0.5183→v1.0.5185 三连发修正。
- **实际正确颜色**：
  - 红+黄：火焰祈咒、沸腾
  - 红+紫：熔岩武器、烈焰战旗
  - 红+青：点燃
  - 红+橙：炽焰武力
  - 红+红：火环图腾
  - 红+棕：火蛇图腾
  - 红+蓝：**仅** 召唤火元素
- **教训**：
  1. **任何时候不得跳过 Phase 1 执行锚点**——必须先输出原始 paras 索引切片，肉眼确认包含"前置条件：""施展时间："字段后才继续。
  2. **所有颜色必须从 raw paras 定位 + XML run-level 颜色逐个确认**，不得使用批量 body-element 匹配。
  3. **"看起来一致"就是最危险的信号**——全"红+蓝"结果应当立刻警觉，而非信任。
