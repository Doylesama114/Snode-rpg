# 工作规则

## 1. 发布版文档
发布版文档 目录中的文件为正式发布版本，**不得随意更新或修改**。如需更新，需经过明确确认后方可操作。

## 2. 备份区
备份区 目录用于存放历史备份文件，**不得覆盖更新**。每次备份应生成带时间戳的独立文件（格式：文件名_yyyy-MM-dd_HH-mm-ss.html），保留所有历史版本。

## 3. 打包前同步文件
执行打包前，必须先通过 `xcopy /E /Y /I "职业页" "electron-app\职业页\"` 同步根目录 `职业页\` 到 `electron-app\职业页\`，确保 electron-app 内的文件与根目录修改保持一致，否则打包出的 exe 不会包含最新的修改。

## 4. 默认打包版本与Win7特供版
- **默认打包**：使用 Electron 42.x 版本，生成的文件直接替换根目录下的 `斯诺德全职业技能树.exe`，覆盖更新即可。
- **Win7特供版**：如需支持 Windows 7，改用 Electron 22.x 版本打包。打包产物**不得替换根目录的 exe**，应另存为 `斯诺德全职业技能树_Win7特供版.zip`（ZIP 压缩包）保存到根目录。
- 注：Win7特供版已停止维护，上述规则仅作历史参考。

## 5. Node.js spawn 补丁
由于当前 Windows 环境存在系统级限制，Node.js 无法创建子进程的 stdio 管道（spawn EPERM），
已通过 spawn_monkey_patch.js 补丁解决。

- 补丁文件位置：electron-app\spawn_monkey_patch.js
- 工作原理：自动将所有 spawn/execFile/exec 调用中的 pipe stdio 替换为 inherit
- 修改位置：electron-app\package.json 中 dist 脚本已添加 --require ./spawn_monkey_patch.js
- 使用方式：正常执行 npm run dist 即可，补丁自动加载（永久生效，无需额外操作）
- 如需临时绕过补丁，可直接调用 node node_modules/electron-builder/out/cli/cli.js --win portable（注意：无补丁时 spawn 管道会失败）

## 6. 完整打包流程（当前环境）

⚠️ **重要：必须使用完整的 electron-builder 管道打包，禁用 `--prepackaged` 方式。**
手动构建的 `resources/app/` 目录会导致 Electron Mojo IPC 通道初始化失败（拒绝访问），
只有通过 electron-builder 自动生成 `app.asar` 的完整流程才能正常工作。

### 打包前准备
1. 修改 `职业页\` 下的文件（唯一修改入口）
2. 执行 `xcopy /E /Y /I "职业页" "electron-app\职业页\"` 同步文件
3. 确保 `electron-app\package.json` 开头**没有 UTF-8 BOM**（字节序标记），否则 electron-builder 会报 JSON 解析错误

### 打包命令（完整 electron-builder 管道）
```powershell
# 1. 清理旧的构建产物
Remove-Item -Recurse -Force electron-app\dist -ErrorAction SilentlyContinue

# 2. 运行完整 electron-builder 管道（自动创建 app.asar + 编译 NSIS 便携版）
cd electron-app
npm run dist
```

⚠️ **不要使用 `--prepackaged` 参数**。必须直接运行 `npm run dist`，让 electron-builder 走完整的"打包→生成 asar→编译 NSIS"流程。

### 打包产物
- 便携版 exe：`electron-app\dist\斯诺德全职业技能树.exe`
- 将 exe 复制到根目录：`Copy-Item electron-app\dist\斯诺德全职业技能树.exe ..\斯诺德全职业技能树_v1.xxx.exe`

### 补丁说明
- 补丁文件：`electron-app\spawn_monkey_patch.js`
- 自动通过 `--require` 加载，`npm run dist` 即可正常使用
- 补丁功能：
  - 将 `child_process.spawn` 的 pipe stdio 替换为文件描述符 + 代理流
  - 拦截 `builder-util.spawnAndWrite`，将 makensis 的 stdin 脚本重定向为临时 .nsi 文件
  - 自动添加 `!addincludedir` 确保 makensis 能找到 NSIS 模板文件
- 如果构建时报 `Unexpected token` 错误，检查 `package.json` 是否有 BOM，用无 BOM 编码重新保存
